# 모듈 통계 분석: zordms0360150

## 1. 함수 구성

### 1.1 함수 개수
**총 함수 개수: 7개** (메인 함수 포함)

### 1.2 함수별 입력값

| 번호 | 함수명 | 입력 파라미터 | 설명 |
|------|--------|--------------|------|
| 1 | `zordms0360150` | 입력 | 메인 함수 |
| 2 | `a000_input_validation` | 입력 | 입력 검증 (내용 생략) |
| 3 | `b000_main_proc` | 입력 | 메인 처리 - 상품 목록 순회 |
| 4 | `요금제_Rule_처리` | 상품 | 요금제 상품 Rule 처리 |
| 5 | `부가서비스_Rule_처리` | 상품 | 부가서비스 상품 Rule 처리 |
| 6 | `f_ordrs03f0000855_check` | 상품 | 선택약정할인 Rule |
| 7 | `f_ordrs03f0000866_check` | 상품 | T지원금약정 해지 Rule |
| 8 | `f_ordrs03f0000924_check` | 상품 | 분실폰찾기 단말체크 Rule |
| 9 | `f_logcp00001_check` | 상품 | 상품공통 Rule (LOGCP00001) |

**참고**: 실제 모듈에는 300-500개의 개별 상품 Rule 함수가 존재하지만, pseudocode에는 대표 함수만 포함

---

## 2. Rule 구성

### 2.1 IF-THEN 구문 분석

#### 2.1.1 함수별 IF-THEN 개수

| 함수명 | IF-THEN 개수 | NESTED IF 개수 | 총 IF 개수 |
|--------|-------------|---------------|-----------|
| `zordms0360150` | 0 | 0 | 0 |
| `a000_input_validation` | 0 | 0 | 0 |
| `b000_main_proc` | 2 | 0 | 2 |
| `요금제_Rule_처리` | 1 | 0 | 1 |
| `부가서비스_Rule_처리` | 3 | 0 | 3 |
| `f_ordrs03f0000855_check` | 1 | 0 | 1 |
| `f_ordrs03f0000866_check` | 4 | 0 | 4 |
| `f_ordrs03f0000924_check` | 4 | 6 | 10 |
| `f_logcp00001_check` | 1 | 2 | 3 |

#### 2.1.2 IF-THEN 상세 분석

**b000_main_proc (2개)**
1. `IF 상품타입 = "G1"` (line 22)
2. `ELSE IF 상품타입 = "I2"` (line 24)

**요금제_Rule_처리 (1개)**
1. `IF 상품.LOGCP코드 = "LOGCP00005" OR "LOGCP00004"` (line 37)

**부가서비스_Rule_처리 (3개)**
1. `IF 상품.상품ID = "NA00004430"` (line 48)
2. `ELSE IF 상품.상품ID = "NA00004487"` (line 50)
3. `ELSE IF 상품.상품ID = "NA00000924"` (line 52)

**f_ordrs03f0000855_check (1개)**
1. `IF 약정정보.약정존재여부 = "Y" AND 약정정보.기준상품가입여부 = "Y"` (line 68)

**f_ordrs03f0000866_check (4개)**
1. `IF 개통후경과일수 <= 14 AND ...` (line 89-93)
2. `ELSE IF 약정정보.해지가능여부 = "Y" AND ...` (line 98-100)
3. `ELSE IF 상품.변경코드 IN ("Z1", "Z2", "C1", "C2")` (line 106)
4. `ELSE` (line 111)

**f_ordrs03f0000924_check (10개 = 4개 기본 + 6개 NESTED)**
1. `IF 상품.가입해지구분 = "01"` (line 127)
   - NESTED: `IF 상품.변경코드 = "C1" OR "C2"` (line 129)
   - NESTED: `IF 명의단말변경여부 = FALSE` (line 132)
   - NESTED: `IF 단말속성.LNUP상세항목 = "02"` (line 137)
   - NESTED: `IF 단말속성.LNUP상세항목 = "02"` (line 146)
2. `ELSE IF 상품.가입해지구분 = "09"` (line 155)
   - NESTED: `IF 명의단말변경여부 = FALSE` (line 159)
   - NESTED: `IF 단말속성.LNUP상세항목 = "02"` (line 164)
3. `ELSE` (line 168) - 내부에 추가 IF 존재
   - NESTED 내부: `IF 기변전단말속성.LNUP상세항목 = "02"` (line 170)

**f_logcp00001_check (3개 = 1개 기본 + 2개 NESTED)**
1. `IF 상품관계 = NOT_FOUND` (line 197)
2. FOR EACH 내부 (line 202-218)
   - NESTED: `IF 관계.관계타입 = "필수"` (line 204)
   - NESTED: `IF 관계.대상상품 미가입` (line 205)
3. `ELSE IF 관계.관계타입 = "배타"` (line 210)
   - NESTED: `IF 관계.대상상품 가입중` (line 211)

#### 2.1.3 IF-THEN 총계
- **기본 IF-THEN**: 16개
- **NESTED IF-THEN**: 8개
- **총 IF-THEN**: 24개

---

### 2.2 DB조회 분석

#### 2.2.1 함수별 DB조회 개수

| 함수명 | DB조회 개수 | 조회 테이블 |
|--------|-----------|-----------|
| `f_ordrs03f0000855_check` | 1 | 약정할인이력 |
| `f_ordrs03f0000866_check` | 1 | 약정할인이력 |
| `f_ordrs03f0000924_check` | 3 | 단말속성, 명의단말변경코드, 기변전단말속성 |
| `f_logcp00001_check` | 1 | 상품관계 |
| **총계** | **6** | - |

#### 2.2.2 DB조회 상세 목록

**f_ordrs03f0000855_check (1개)**
1. `DB조회("약정할인이력", 상품.서비스관리번호)` (line 66)

**f_ordrs03f0000866_check (1개)**
1. `DB조회("약정할인이력", 상품.서비스관리번호)` (line 86)

**f_ordrs03f0000924_check (3개)**
1. `DB조회("단말속성", 상품.단말모델코드, "LNUP항목=544")` (line 125)
2. `DB조회("명의단말변경코드", 상품.변경사유코드)` (line 130, 157)
   - 2회 조회 (line 130, line 157)
3. `DB조회("기변전단말속성", "LNUP항목=544")` (line 168)

**f_logcp00001_check (1개)**
1. `DB조회("상품관계", "LOGCP00001", 상품.상품ID)` (line 195)

---

### 2.3 IF-THEN과 DB조회를 모두 포함하는 Rule

#### 2.3.1 정의
IF-THEN 조건문 내부에 DB조회가 포함되거나, DB조회 결과를 IF-THEN에서 사용하는 경우

#### 2.3.2 해당 Rule 목록

**f_ordrs03f0000855_check (1개)**

1. **약정 정보 조회 후 체크** (line 66-76)
   ```
   약정정보 ← DB조회("약정할인이력", 상품.서비스관리번호)
   IF 약정정보.약정존재여부 = "Y" AND 약정정보.기준상품가입여부 = "Y" THEN
   ```

**f_ordrs03f0000866_check (1개)**

1. **약정 정보 조회 후 조건 체크** (line 86-115)
   ```
   약정정보 ← DB조회("약정할인이력", 상품.서비스관리번호)
   IF 개통후경과일수 <= 14 AND 약정정보.약정존재여부 = "Y" AND ...
   ELSE IF 약정정보.해지가능여부 = "Y" AND ...
   ```

**f_ordrs03f0000924_check (3개)**

1. **단말 속성 조회 후 체크** (line 125-153)
   ```
   단말속성 ← DB조회("단말속성", 상품.단말모델코드, "LNUP항목=544")
   IF 상품.가입해지구분 = "01" THEN
       IF 단말속성.LNUP상세항목 = "02" THEN
   ```

2. **명의단말변경 조회 후 체크** (line 130-143, 157-162)
   ```
   명의단말변경여부 ← DB조회("명의단말변경코드", 상품.변경사유코드)
   IF 명의단말변경여부 = FALSE THEN
   ```

3. **기변전단말 속성 조회 후 체크** (line 168-180)
   ```
   기변전단말속성 ← DB조회("기변전단말속성", "LNUP항목=544")
   IF 기변전단말속성.LNUP상세항목 = "02" THEN
   ```

**f_logcp00001_check (1개)**

1. **상품관계 조회 후 체크** (line 195-221)
   ```
   상품관계 ← DB조회("상품관계", "LOGCP00001", 상품.상품ID)
   IF 상품관계 = NOT_FOUND THEN
   FOR EACH 관계 IN 상품관계
       IF 관계.관계타입 = "필수" THEN
       IF 관계.관계타입 = "배타" THEN
   ```

#### 2.3.3 IF-THEN + DB조회 Rule 총계
**총 6개**

---

## 3. 통계 요약

| 항목 | 개수 | 비고 |
|------|------|------|
| **함수 개수** | 7 | 메인 함수 포함 (실제 300-500개) |
| **IF-THEN 총 개수** | 24 | 기본 16개 + NESTED 8개 |
| **DB조회 총 개수** | 6 | 중복 조회 포함 시 7개 |
| **IF-THEN + DB조회 Rule** | 6 | 모든 DB조회가 조건부 |

---

## 4. 함수별 복잡도 분석

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수* |
|--------|---------|--------|-----------|-------------|
| `f_ordrs03f0000924_check` | 10 | 3 | 3 | 16 |
| `f_ordrs03f0000866_check` | 4 | 1 | 1 | 6 |
| `부가서비스_Rule_처리` | 3 | 0 | 0 | 3 |
| `f_logcp00001_check` | 3 | 1 | 1 | 5 |
| `b000_main_proc` | 2 | 0 | 0 | 2 |
| `f_ordrs03f0000855_check` | 1 | 1 | 1 | 3 |
| `요금제_Rule_처리` | 1 | 0 | 0 | 1 |
| 기타 함수 | 0 | 0 | 0 | 0 |

*복잡도 점수 = IF-THEN + DB조회 + IF+DB Rule

---

## 5. 조회 테이블 통계

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| 약정할인이력 | 2 | f_ordrs03f0000855_check, f_ordrs03f0000866_check |
| 단말속성 | 1 | f_ordrs03f0000924_check |
| 명의단말변경코드 | 2 | f_ordrs03f0000924_check (×2) |
| 기변전단말속성 | 1 | f_ordrs03f0000924_check |
| 상품관계 | 1 | f_logcp00001_check |

**총 테이블 수: 5개**

---

## 6. Rule 패턴 분석

### 6.1 Rule 유형별 분류

| Rule 유형 | 개수 | 함수 예시 |
|----------|------|----------|
| **약정 기반 Rule** | 2 | f_ordrs03f0000855_check, f_ordrs03f0000866_check |
| **단말 호환성 Rule** | 1 | f_ordrs03f0000924_check |
| **상품 관계 Rule** | 1 | f_logcp00001_check |
| **분기 처리 Rule** | 2 | b000_main_proc, 부가서비스_Rule_처리 |

### 6.2 복잡도 특성

**고복잡도 함수 (복잡도 10 이상)**
- `f_ordrs03f0000924_check`: 복잡도 16
  - 다중 가입구분 처리 (신규/사용중)
  - 명의/단말 변경 조건 체크
  - 자동 해지 처리 로직

**중복잡도 함수 (복잡도 5-9)**
- `f_ordrs03f0000866_check`: 복잡도 6
  - 14일 이내 해지 조건
  - 약정 상태 체크
  - 변경코드 예외 처리

**저복잡도 함수 (복잡도 1-4)**
- 나머지 함수들: 단순 분기 또는 단일 조건 체크

### 6.3 DB 조회 패턴

**패턴 1: 단일 조회 후 조건 체크**
- f_ordrs03f0000855_check, f_ordrs03f0000866_check, f_logcp00001_check
- 1회 DB 조회 → 결과 기반 IF-THEN 분기

**패턴 2: 다중 조회 및 복합 조건**
- f_ordrs03f0000924_check
- 3회 DB 조회 (단말속성, 명의단말변경코드×2, 기변전단말속성)
- 조회 결과 조합하여 복잡한 조건 판단

---

## 7. 확장성 분석

### 7.1 실제 모듈 규모 추정

**Pseudocode 기준**
- 함수 개수: 7개
- 총 라인수: 249줄

**실제 모듈 규모 (8,622줄)**
- 추정 함수 개수: 300-500개
- 평균 함수당 라인수: 17-29줄
- 상품별 Rule 함수가 대부분

### 7.2 Rule 함수 증가 패턴

**신규 상품 추가 시**
- 부가서비스_Rule_처리에 IF 분기 1개 추가
- 새로운 f_ordrs03fXXXXXXX_check 함수 1개 추가
- 평균 IF-THEN: 2-5개
- 평균 DB조회: 1-2개

**예상 통계 (전체 모듈)**
- 총 IF-THEN: 약 1,000-2,500개
- 총 DB조회: 약 300-1,000개
- IF+DB Rule: 약 300-1,000개

---

## 8. 성능 영향 분석

### 8.1 DB 조회 부하

| 조회 유형 | 빈도 | 성능 영향 |
|----------|------|----------|
| 약정할인이력 | 높음 | 중간 (인덱스 필수) |
| 단말속성 | 중간 | 낮음 (캐싱 가능) |
| 명의단말변경코드 | 낮음 | 낮음 (마스터 데이터) |
| 상품관계 | 높음 | 낮음 (캐싱 가능) |

### 8.2 최적화 권장사항

1. **약정할인이력 테이블**
   - 서비스관리번호 인덱스 필수
   - 실시간 조회 필요 (캐싱 불가)

2. **단말속성/상품관계 테이블**
   - 마스터 데이터 → 캐싱 적용 가능
   - 변경 빈도 낮음

3. **병렬 처리**
   - 상품별 Rule 함수는 독립적
   - 멀티스레드 처리 가능

---

## 9. 버전 정보

- **분석 일자**: 2026-03-31
- **분석자**: Bob (Documentation Writer)
- **문서 버전**: 1.0
- **기반 Pseudocode**: zordms0360150.txt (249 lines, 실제 8,622줄)
- **분석 범위**: 대표 함수 7개 (실제 300-500개 함수 존재)