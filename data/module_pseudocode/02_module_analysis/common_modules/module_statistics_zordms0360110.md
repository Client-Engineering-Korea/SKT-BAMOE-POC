# 모듈 통계 분석: zordms0360110

## 1. 함수 구성

### 1.1 함수 개수
**총 함수 개수: 10개**

### 1.2 함수별 입력값

| 번호 | 함수명 | 입력 파라미터 | 설명 |
|------|--------|--------------|------|
| 1 | `zordms0360110` | 입력 | 메인 함수 |
| 2 | `a000_input_validation` | 입력 | 입력 검증 (내용 생략) |
| 3 | `b000_main_proc` | 입력 | 메인 처리 |
| 4 | `부가서비스_가입체크` | 상품 | 부가서비스 가입 체크 |
| 5 | `부가서비스_해지체크` | 상품 | 부가서비스 해지 체크 |
| 6 | `상품별_가입Rule_처리` | 상품 | 상품별 특화 Rule 처리 |
| 7 | `통화중대기_가입Rule` | 상품 | 통화중대기 서비스 가입 Rule |
| 8 | `선택약정할인_가입Rule` | 상품 | 선택약정할인 서비스 가입 Rule |
| 9 | `계산위약금` | 약정정보 | 약정 위약금 계산 |
| 10 | `계산혜택반환금액` | 프로모션정보 | 프로모션 혜택 반환 금액 계산 |
| 11 | `z000_norm_exit_proc` | 입력 | 정상 종료 처리 (내용 생략) |
| 12 | `z999_err_exit_proc` | 입력 | 오류 종료 처리 (내용 생략) |

**참고**: 
- 실제 함수 개수는 12개 (종료 처리 함수 포함)
- pseudocode에는 수백 개의 상품별 Rule 함수가 존재하나, 예시로 2개만 표시됨
- 대용량 모듈(27,000줄 이상)의 특성상 전체 함수 목록은 생략됨

---

## 2. Rule 구성

### 2.1 IF-THEN 구문 분석

#### 2.1.1 함수별 IF-THEN 개수

| 함수명 | IF-THEN 개수 | NESTED IF 개수 | 총 IF 개수 |
|--------|-------------|---------------|-----------|
| `zordms0360110` | 0 | 0 | 0 |
| `b000_main_proc` | 2 | 0 | 2 |
| `부가서비스_가입체크` | 8 | 3 | 11 |
| `부가서비스_해지체크` | 6 | 4 | 10 |
| `상품별_가입Rule_처리` | 5 | 0 | 5 |
| `통화중대기_가입Rule` | 2 | 0 | 2 |
| `선택약정할인_가입Rule` | 2 | 0 | 2 |
| `계산위약금` | 2 | 0 | 2 |
| `계산혜택반환금액` | 1 | 0 | 1 |

#### 2.1.2 IF-THEN 상세 분석

**b000_main_proc (2개)**
1. `IF 가입해지구분 = "01"` (line 28)
2. `ELSE IF 가입해지구분 = "03"` (line 30)

**부가서비스_가입체크 (11개 = 8개 기본 + 3개 NESTED)**
1. `IF 상품ID LIKE "NA%"` (line 48)
2. `IF 기존가입 EXISTS AND 기존가입.상태 = "가입중"` (line 58)
3. `IF 기준요금제목록.건수 > 0` (line 68)
   - NESTED: `IF 현재요금제.요금제ID NOT IN 기준요금제목록` (line 71)
4. FOR EACH 선행서비스 (line 82)
   - NESTED: `IF 가입여부 = "미가입"` (line 85)
5. FOR EACH 배타서비스 (line 96)
   - NESTED: `IF 가입여부 = "가입중"` (line 99)
6. `IF 상품정보.최소나이 > 0 AND 고객.나이 < 상품정보.최소나이` (line 110)
7. `IF 상품정보.최대나이 > 0 AND 고객.나이 > 상품정보.최대나이` (line 116)
8. `IF 상품정보.단말제한 = "Y"` (line 124)
   - NESTED: `IF 단말속성.지원여부 = "N"` (line 128)

**부가서비스_해지체크 (10개 = 6개 기본 + 4개 NESTED)**
1. `IF 기존가입 = NOT_FOUND OR 기존가입.상태 = "해지"` (line 150)
2. `IF 기존가입.최소사용기간 > 0` (line 158)
   - NESTED: `IF 사용기간 < 기존가입.최소사용기간` (line 161)
3. FOR EACH 종속서비스 (line 172)
   - NESTED: `IF 종속서비스.상태 = "가입중"` (line 173)
4. `IF 기존가입.약정여부 = "Y"` (line 182)
   - NESTED: `IF 약정정보 EXISTS AND 약정정보.약정상태 = "유효"` (line 185)
   - NESTED: `IF 위약금 > 0` (line 188)
5. `IF 프로모션정보 EXISTS AND 프로모션정보.상태 = "유효"` (line 199)
   - NESTED: `IF 혜택반환금액 > 0` (line 202)

**상품별_가입Rule_처리 (5개)**
1. `IF 상품ID = "NA00000292"` (line 224)
2. `ELSE IF 상품ID = "NA00001234"` (line 227)
3. `ELSE IF 상품ID = "NA00004430"` (line 230)
4. `ELSE IF 상품ID = "NA00004487"` (line 233)
5. `ELSE` (line 238) - 기본 Rule

**통화중대기_가입Rule (2개)**
1. `IF 현재요금제.세대 < "4G"` (line 255)
2. `IF 단말속성.지원여부 = "N"` (line 265)

**선택약정할인_가입Rule (2개)**
1. `IF 약정정보 = NOT_FOUND OR 약정정보.약정존재여부 = "N"` (line 284)
2. `IF 약정정보.기준상품가입여부 = "N"` (line 291)

**계산위약금 (2개)**
1. `IF 남은개월수 <= 0` (line 309)
2. `IF 경과개월수 < 12` (line 313)

**계산혜택반환금액 (1개)**
1. `IF 경과개월수 >= 최소사용개월수` (line 329)

#### 2.1.3 IF-THEN 총계
- **기본 IF-THEN**: 28개
- **NESTED IF-THEN**: 7개
- **총 IF-THEN**: 35개

**참고**: 대용량 모듈의 특성상 수백 개의 상품별 Rule 함수가 추가로 존재하며, 실제 IF-THEN 개수는 훨씬 많음

---

### 2.2 DB조회 분석

#### 2.2.1 함수별 DB조회 개수

| 함수명 | DB조회 개수 | 조회 테이블 |
|--------|-----------|-----------|
| `부가서비스_가입체크` | 8 | 고객가입상품, 부가서비스기준요금제, 고객현재요금제, 선행부가서비스, 고객가입여부(×2), 배타부가서비스, 상품정보, 고객현재단말, 단말속성 |
| `부가서비스_해지체크` | 3 | 고객가입상품, 종속부가서비스, 상품약정정보, 상품프로모션 |
| `통화중대기_가입Rule` | 3 | 고객현재요금제, 고객현재단말, 단말속성 |
| `선택약정할인_가입Rule` | 1 | 고객약정정보 |
| **총계** | **15** | - |

#### 2.2.2 DB조회 상세 목록

**부가서비스_가입체크 (8개)**
1. `DB조회("고객가입상품", 고객.서비스관리번호, 상품ID)` (line 56)
2. `DB조회("부가서비스기준요금제", 상품ID)` (line 66)
3. `DB조회("고객현재요금제", 고객.서비스관리번호)` (line 69)
4. `DB조회("선행부가서비스", 상품ID)` (line 80)
5. `DB조회("고객가입여부", 고객.서비스관리번호, 선행서비스.상품ID)` (line 83) - FOR EACH 내부
6. `DB조회("배타부가서비스", 상품ID)` (line 94)
7. `DB조회("고객가입여부", 고객.서비스관리번호, 배타서비스.상품ID)` (line 97) - FOR EACH 내부
8. `DB조회("상품정보", 상품ID)` (line 108)
9. `DB조회("고객현재단말", 고객.서비스관리번호)` (line 125)
10. `DB조회("단말속성", 단말정보.단말모델코드, 상품ID)` (line 126)

**부가서비스_해지체크 (4개)**
1. `DB조회("고객가입상품", 고객.서비스관리번호, 상품.상품ID)` (line 148)
2. `DB조회("종속부가서비스", 상품.상품ID, 고객.서비스관리번호)` (line 170)
3. `DB조회("상품약정정보", 고객.서비스관리번호, 상품.상품ID)` (line 183)
4. `DB조회("상품프로모션", 고객.서비스관리번호, 상품.상품ID)` (line 197)

**통화중대기_가입Rule (3개)**
1. `DB조회("고객현재요금제", 고객.서비스관리번호)` (line 253)
2. `DB조회("고객현재단말", 고객.서비스관리번호)` (line 262)
3. `DB조회("단말속성", 단말정보.단말모델코드, "통화중대기")` (line 263)

**선택약정할인_가입Rule (1개)**
1. `DB조회("고객약정정보", 고객.서비스관리번호)` (line 282)

---

### 2.3 IF-THEN과 DB조회를 모두 포함하는 Rule

#### 2.3.1 정의
IF-THEN 조건문 내부에 DB조회가 포함되거나, DB조회 결과를 IF-THEN에서 사용하는 경우

#### 2.3.2 해당 Rule 목록

**부가서비스_가입체크 (6개)**

1. **중복 가입 체크** (line 56-62)
   ```
   기존가입 ← DB조회("고객가입상품", 고객.서비스관리번호, 상품ID)
   IF 기존가입 EXISTS AND 기존가입.상태 = "가입중" THEN
   ```

2. **기준 요금제 체크** (line 66-76)
   ```
   기준요금제목록 ← DB조회("부가서비스기준요금제", 상품ID)
   IF 기준요금제목록.건수 > 0 THEN
       현재요금제 ← DB조회("고객현재요금제", 고객.서비스관리번호)
       IF 현재요금제.요금제ID NOT IN 기준요금제목록 THEN
   ```

3. **선행 서비스 체크** (line 80-90)
   ```
   선행서비스목록 ← DB조회("선행부가서비스", 상품ID)
   FOR EACH 선행서비스 IN 선행서비스목록
       가입여부 ← DB조회("고객가입여부", ...)
       IF 가입여부 = "미가입" THEN
   ```

4. **배타 서비스 체크** (line 94-104)
   ```
   배타서비스목록 ← DB조회("배타부가서비스", 상품ID)
   FOR EACH 배타서비스 IN 배타서비스목록
       가입여부 ← DB조회("고객가입여부", ...)
       IF 가입여부 = "가입중" THEN
   ```

5. **나이 제한 체크** (line 108-120)
   ```
   상품정보 ← DB조회("상품정보", 상품ID)
   IF 상품정보.최소나이 > 0 AND 고객.나이 < 상품정보.최소나이 THEN
   IF 상품정보.최대나이 > 0 AND 고객.나이 > 상품정보.최대나이 THEN
   ```

6. **단말 제한 체크** (line 124-133)
   ```
   IF 상품정보.단말제한 = "Y" THEN
       단말정보 ← DB조회("고객현재단말", 고객.서비스관리번호)
       단말속성 ← DB조회("단말속성", 단말정보.단말모델코드, 상품ID)
       IF 단말속성.지원여부 = "N" THEN
   ```

**부가서비스_해지체크 (4개)**

1. **가입 여부 체크** (line 148-154)
   ```
   기존가입 ← DB조회("고객가입상품", 고객.서비스관리번호, 상품.상품ID)
   IF 기존가입 = NOT_FOUND OR 기존가입.상태 = "해지" THEN
   ```

2. **종속 서비스 체크** (line 170-178)
   ```
   종속서비스목록 ← DB조회("종속부가서비스", 상품.상품ID, 고객.서비스관리번호)
   FOR EACH 종속서비스 IN 종속서비스목록
       IF 종속서비스.상태 = "가입중" THEN
   ```

3. **약정 체크** (line 182-193)
   ```
   IF 기존가입.약정여부 = "Y" THEN
       약정정보 ← DB조회("상품약정정보", 고객.서비스관리번호, 상품.상품ID)
       IF 약정정보 EXISTS AND 약정정보.약정상태 = "유효" THEN
   ```

4. **프로모션 혜택 체크** (line 197-206)
   ```
   프로모션정보 ← DB조회("상품프로모션", 고객.서비스관리번호, 상품.상품ID)
   IF 프로모션정보 EXISTS AND 프로모션정보.상태 = "유효" THEN
   ```

**통화중대기_가입Rule (2개)**

1. **요금제 체크** (line 253-259)
   ```
   현재요금제 ← DB조회("고객현재요금제", 고객.서비스관리번호)
   IF 현재요금제.세대 < "4G" THEN
   ```

2. **단말 체크** (line 262-269)
   ```
   단말정보 ← DB조회("고객현재단말", 고객.서비스관리번호)
   단말속성 ← DB조회("단말속성", 단말정보.단말모델코드, "통화중대기")
   IF 단말속성.지원여부 = "N" THEN
   ```

**선택약정할인_가입Rule (1개)**

1. **약정 존재 여부 체크** (line 282-288)
   ```
   약정정보 ← DB조회("고객약정정보", 고객.서비스관리번호)
   IF 약정정보 = NOT_FOUND OR 약정정보.약정존재여부 = "N" THEN
   ```

#### 2.3.3 IF-THEN + DB조회 Rule 총계
**총 13개**

---

## 3. 통계 요약

| 항목 | 개수 | 비고 |
|------|------|------|
| **함수 개수** | 10 | 주요 함수만 집계 (상품별 Rule 함수 제외) |
| **IF-THEN 총 개수** | 35 | 기본 28개 + NESTED 7개 |
| **DB조회 총 개수** | 15 | 중복 조회 포함 |
| **IF-THEN + DB조회 Rule** | 13 | 조건부 DB조회 포함 |

**참고**: 
- 대용량 모듈(27,000줄 이상)의 특성상 수백 개의 상품별 Rule 함수가 추가로 존재
- 실제 함수 개수, IF-THEN 개수, DB조회 개수는 훨씬 많음
- 본 통계는 pseudocode에 명시된 주요 함수만 집계

---

## 4. 함수별 복잡도 분석

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수* |
|--------|---------|--------|-----------|-------------|
| `부가서비스_가입체크` | 11 | 8 | 6 | 25 |
| `부가서비스_해지체크` | 10 | 4 | 4 | 18 |
| `상품별_가입Rule_처리` | 5 | 0 | 0 | 5 |
| `통화중대기_가입Rule` | 2 | 3 | 2 | 7 |
| `선택약정할인_가입Rule` | 2 | 1 | 1 | 4 |
| `b000_main_proc` | 2 | 0 | 0 | 2 |
| `계산위약금` | 2 | 0 | 0 | 2 |
| `계산혜택반환금액` | 1 | 0 | 0 | 1 |
| 기타 함수 | 0 | 0 | 0 | 0 |

*복잡도 점수 = IF-THEN + DB조회 + IF+DB Rule

---

## 5. 조회 테이블 통계

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| 고객가입여부 | 2 | 부가서비스_가입체크 (FOR EACH 내부 ×2) |
| 고객현재요금제 | 2 | 부가서비스_가입체크, 통화중대기_가입Rule |
| 고객현재단말 | 2 | 부가서비스_가입체크, 통화중대기_가입Rule |
| 단말속성 | 2 | 부가서비스_가입체크, 통화중대기_가입Rule |
| 고객가입상품 | 2 | 부가서비스_가입체크, 부가서비스_해지체크 |
| 고객약정정보 | 2 | 부가서비스_해지체크, 선택약정할인_가입Rule |
| 부가서비스기준요금제 | 1 | 부가서비스_가입체크 |
| 선행부가서비스 | 1 | 부가서비스_가입체크 |
| 배타부가서비스 | 1 | 부가서비스_가입체크 |
| 상품정보 | 1 | 부가서비스_가입체크 |
| 종속부가서비스 | 1 | 부가서비스_해지체크 |
| 상품약정정보 | 1 | 부가서비스_해지체크 |
| 상품프로모션 | 1 | 부가서비스_해지체크 |

**총 테이블 수: 13개**

---

## 6. 대용량 모듈 특성

### 6.1 상품별 Rule 함수
- **예상 함수 수**: 수백 개 (pseudocode에는 4개 예시만 표시)
- **상품 ID 패턴**: "NA00000292", "NA00001234", "NA00004430", "NA00004487" 등
- **각 함수 특징**: 
  - 평균 IF-THEN: 2~5개
  - 평균 DB조회: 1~3개
  - 평균 복잡도: 3~8

### 6.2 예상 총 통계 (상품별 Rule 포함)
- **총 함수 개수**: 300~500개 (추정)
- **총 IF-THEN**: 1,000~2,000개 (추정)
- **총 DB조회**: 500~1,000개 (추정)
- **총 복잡 Rule**: 400~800개 (추정)

---

## 7. 버전 정보

- **분석 일자**: 2026-03-31
- **분석자**: Bob (Documentation Writer)
- **문서 버전**: 1.0
- **기반 Pseudocode**: zordms0360110.txt (366 lines, 대용량 모듈 27,000줄 이상)
- **분석 범위**: 주요 함수 및 예시 함수만 집계 (전체 상품별 Rule 제외)