# 모듈 통계 분석: zordms0360120

## 1. 함수 구성

### 1.1 함수 개수
**총 함수 개수: 10개**

### 1.2 함수별 입력값

| 번호 | 함수명 | 입력 파라미터 | 설명 |
|------|--------|--------------|------|
| 1 | `zordms0360120` | 입력 | 메인 함수 |
| 2 | `a000_input_validation` | 입력 | 입력 검증 |
| 3 | `b000_main_proc` | 입력 | 메인 처리 |
| 4 | `요금제_가입체크` | 요금제 | 신규 가입 시 요금제 사용 가능 여부 체크 |
| 5 | `요금제_변경체크` | 요금제 | 요금제 변경 시 사용 가능 여부 체크 |
| 6 | `요금제_해지체크` | 요금제 | 요금제 해지 시 체크 |
| 7 | `계산위약금` | 약정정보, 경과개월수 | 약정 위약금 계산 |
| 8 | `p010_set_initial_val` | 입력 | 초기값 설정 |
| 9 | `p100_chk_mndt_item` | 입력 | 필수 항목 체크 |
| 10 | `p200_chk_mtcg_item` | 입력 | 선택 항목 체크 |
| 11 | `z000_norm_exit_proc` | 입력 | 정상 종료 처리 (내용 생략) |
| 12 | `z999_err_exit_proc` | 입력 | 오류 종료 처리 (내용 생략) |

**참고**: 실제 함수 개수는 12개 (종료 처리 함수 포함)

---

## 2. Rule 구성

### 2.1 IF-THEN 구문 분석

#### 2.1.1 함수별 IF-THEN 개수

| 함수명 | IF-THEN 개수 | NESTED IF 개수 | 총 IF 개수 |
|--------|-------------|---------------|-----------|
| `zordms0360120` | 0 | 0 | 0 |
| `a000_input_validation` | 0 | 0 | 0 |
| `b000_main_proc` | 3 | 0 | 3 |
| `요금제_가입체크` | 8 | 6 | 14 |
| `요금제_변경체크` | 10 | 4 | 14 |
| `요금제_해지체크` | 3 | 2 | 5 |
| `계산위약금` | 3 | 4 | 7 |
| `p010_set_initial_val` | 0 | 0 | 0 |
| `p100_chk_mndt_item` | 3 | 0 | 3 |
| `p200_chk_mtcg_item` | 2 | 0 | 2 |

#### 2.1.2 IF-THEN 상세 분석

**b000_main_proc (3개)**
1. `IF 요금제.가입해지구분 = "01"` (line 35)
2. `ELSE IF 요금제.가입해지구분 = "02"` (line 37)
3. `ELSE IF 요금제.가입해지구분 = "03"` (line 39)

**요금제_가입체크 (14개 = 8개 기본 + 6개 NESTED)**
1. `IF 요금제정보 = NOT_FOUND` (line 61)
2. `IF 요금제정보.판매가능여부 = "N"` (line 69)
3. `IF 입력.처리일자 < 요금제정보.판매시작일 OR ...` (line 77)
4. `IF 요금제정보.최소나이 > 0` (line 86)
   - NESTED: `IF 고객.나이 < 요금제정보.최소나이` (line 87)
5. `IF 요금제정보.최대나이 > 0` (line 94)
   - NESTED: `IF 고객.나이 > 요금제정보.최대나이` (line 95)
6. `IF 요금제정보.채널제한 = "Y"` (line 104)
   - NESTED: `IF 입력.채널 NOT IN 허용채널목록` (line 107)
7. `IF 요금제정보.단말세대제한 = "Y"` (line 116)
   - NESTED: `IF 단말정보.세대 < 요금제정보.요구세대` (line 119)
8. `IF 요금제정보.USIM타입제한 = "Y"` (line 128)
   - NESTED: `IF USIM정보.타입 ≠ 요금제정보.요구USIM타입` (line 131)
9. `IF 요금제정보.지역제한 = "Y"` (line 140)
   - NESTED: `IF 지역코드 NOT IN 허용지역목록` (line 144)

**요금제_변경체크 (14개 = 10개 기본 + 4개 NESTED)**
1. `IF 기존요금제 = NOT_FOUND` (line 170)
2. `IF 기존요금제.요금제ID = 신규요금제ID` (line 178)
3. `IF 출력.사용가능여부 = "N"` (line 187)
4. `IF 기존요금제.최소사용기간 > 0` (line 193)
   - NESTED: `IF 사용기간 < 기존요금제.최소사용기간` (line 196)
5. `IF 변경이력.변경횟수 >= 3` (line 207)
6. `IF 기존요금제.세대 = "3G" AND 신규요금제정보.세대 = "5G"` (line 217)
   - NESTED: `IF 직접변경불가규칙 = "Y"` (line 218)
7. `IF 변경제한 EXISTS AND 변경제한.변경불가 = "Y"` (line 229)
8. `IF 약정정보 EXISTS AND 약정정보.약정상태 = "유효"` (line 239)
   - NESTED: `IF 남은개월수 > 0` (line 244)
   - NESTED: `IF 위약금 > 0` (line 248)
9. `IF 신규요금제정보.기본료 > 기존요금제.기본료 × 1.3` (line 256)
10. `ELSE IF 신규요금제정보.기본료 < 기존요금제.기본료 × 0.7` (line 258)

**요금제_해지체크 (5개 = 3개 기본 + 2개 NESTED)**
1. `IF 기존요금제 = NOT_FOUND` (line 278)
2. `IF 기존요금제.최소사용기간 > 0` (line 286)
   - NESTED: `IF 사용기간 < 기존요금제.최소사용기간` (line 289)
3. `IF 약정정보 EXISTS AND 약정정보.약정상태 = "유효"` (line 300)
4. `IF 부가서비스목록.건수 > 0` (line 310)

**계산위약금 (7개 = 3개 기본 + 4개 NESTED)**
1. `IF 경과개월수 = NULL` (line 326)
2. `IF 남은개월수 <= 0` (line 332)
3. `IF 약정정보.약정타입 = "24개월"` (line 336)
   - NESTED: `IF 경과개월수 < 6` (line 337)
   - NESTED: `ELSE IF 경과개월수 < 12` (line 339)
   - NESTED: `ELSE IF 경과개월수 < 18` (line 341)
4. `ELSE IF 약정정보.약정타입 = "12개월"` (line 347)
   - NESTED: `IF 경과개월수 < 6` (line 348)

**p100_chk_mndt_item (3개)**
1. `IF 입력.서비스관리번호 = NULL` (line 375)
2. `IF 입력.요금제ID = NULL` (line 380)
3. `IF 입력.가입해지구분 = NULL` (line 385)

**p200_chk_mtcg_item (2개)**
1. `IF 입력.처리일자 = NULL` (line 397)
2. `IF 입력.가입해지구분 NOT IN ("01", "02", "03")` (line 401)

#### 2.1.3 IF-THEN 총계
- **기본 IF-THEN**: 32개
- **NESTED IF-THEN**: 16개
- **총 IF-THEN**: 48개

---

### 2.2 DB조회 분석

#### 2.2.1 함수별 DB조회 개수

| 함수명 | DB조회 개수 | 조회 테이블 |
|--------|-----------|-----------|
| `요금제_가입체크` | 5 | 요금제마스터, 요금제판매채널, 단말정보, USIM정보, 요금제판매지역 |
| `요금제_변경체크` | 5 | 고객현재요금제, 요금제변경이력, 요금제마스터, 요금제변경제한, 요금제약정정보 |
| `요금제_해지체크` | 3 | 고객현재요금제, 요금제약정정보, 고객가입부가서비스 |
| **총계** | **13** | - |

#### 2.2.2 DB조회 상세 목록

**요금제_가입체크 (5개)**
1. `DB조회("요금제마스터", 요금제ID, 입력.처리일시)` (line 59)
2. `DB조회("요금제판매채널", 요금제ID)` (line 105)
3. `DB조회("단말정보", 입력.단말모델코드)` (line 117)
4. `DB조회("USIM정보", 입력.USIM일련번호)` (line 129)
5. `DB조회("요금제판매지역", 요금제ID)` (line 142)

**요금제_변경체크 (5개)**
1. `DB조회("고객현재요금제", 고객.서비스관리번호)` (line 168)
2. `DB조회("요금제변경이력", 고객.서비스관리번호, 최근1개월)` (line 205)
3. `DB조회("요금제마스터", 신규요금제ID)` (line 215)
4. `DB조회("요금제변경제한", 기존요금제.요금제ID, 신규요금제ID)` (line 227)
5. `DB조회("요금제약정정보", 고객.서비스관리번호)` (line 237)

**요금제_해지체크 (3개)**
1. `DB조회("고객현재요금제", 고객.서비스관리번호)` (line 276)
2. `DB조회("요금제약정정보", 고객.서비스관리번호)` (line 298)
3. `DB조회("고객가입부가서비스", 고객.서비스관리번호)` (line 308)

---

### 2.3 IF-THEN과 DB조회를 모두 포함하는 Rule

#### 2.3.1 정의
IF-THEN 조건문 내부에 DB조회가 포함되거나, DB조회 결과를 IF-THEN에서 사용하는 경우

#### 2.3.2 해당 Rule 목록

**요금제_가입체크 (5개)**

1. **요금제 정보 조회 후 체크** (line 59-65)
   ```
   요금제정보 ← DB조회("요금제마스터", 요금제ID, 입력.처리일시)
   IF 요금제정보 = NOT_FOUND THEN
   ```

2. **채널 제한 체크** (line 104-112)
   ```
   IF 요금제정보.채널제한 = "Y" THEN
       허용채널목록 ← DB조회("요금제판매채널", 요금제ID)
       IF 입력.채널 NOT IN 허용채널목록 THEN
   ```

3. **단말 세대 체크** (line 116-124)
   ```
   IF 요금제정보.단말세대제한 = "Y" THEN
       단말정보 ← DB조회("단말정보", 입력.단말모델코드)
       IF 단말정보.세대 < 요금제정보.요구세대 THEN
   ```

4. **USIM 타입 체크** (line 128-136)
   ```
   IF 요금제정보.USIM타입제한 = "Y" THEN
       USIM정보 ← DB조회("USIM정보", 입력.USIM일련번호)
       IF USIM정보.타입 ≠ 요금제정보.요구USIM타입 THEN
   ```

5. **지역 제한 체크** (line 140-149)
   ```
   IF 요금제정보.지역제한 = "Y" THEN
       허용지역목록 ← DB조회("요금제판매지역", 요금제ID)
       IF 지역코드 NOT IN 허용지역목록 THEN
   ```

**요금제_변경체크 (5개)**

1. **기존 요금제 조회 후 체크** (line 168-174)
   ```
   기존요금제 ← DB조회("고객현재요금제", 고객.서비스관리번호)
   IF 기존요금제 = NOT_FOUND THEN
   ```

2. **변경 횟수 제한 체크** (line 205-211)
   ```
   변경이력 ← DB조회("요금제변경이력", 고객.서비스관리번호, 최근1개월)
   IF 변경이력.변경횟수 >= 3 THEN
   ```

3. **세대간 변경 제한 체크** (line 215-223)
   ```
   신규요금제정보 ← DB조회("요금제마스터", 신규요금제ID)
   IF 기존요금제.세대 = "3G" AND 신규요금제정보.세대 = "5G" THEN
   ```

4. **변경 제한 관계 체크** (line 227-233)
   ```
   변경제한 ← DB조회("요금제변경제한", 기존요금제.요금제ID, 신규요금제ID)
   IF 변경제한 EXISTS AND 변경제한.변경불가 = "Y" THEN
   ```

5. **약정 체크** (line 237-252)
   ```
   약정정보 ← DB조회("요금제약정정보", 고객.서비스관리번호)
   IF 약정정보 EXISTS AND 약정정보.약정상태 = "유효" THEN
   ```

**요금제_해지체크 (3개)**

1. **기존 요금제 조회 후 체크** (line 276-282)
   ```
   기존요금제 ← DB조회("고객현재요금제", 고객.서비스관리번호)
   IF 기존요금제 = NOT_FOUND THEN
   ```

2. **약정 체크** (line 298-304)
   ```
   약정정보 ← DB조회("요금제약정정보", 고객.서비스관리번호)
   IF 약정정보 EXISTS AND 약정정보.약정상태 = "유효" THEN
   ```

3. **부가서비스 체크** (line 308-312)
   ```
   부가서비스목록 ← DB조회("고객가입부가서비스", 고객.서비스관리번호)
   IF 부가서비스목록.건수 > 0 THEN
   ```

#### 2.3.3 IF-THEN + DB조회 Rule 총계
**총 13개**

---

## 3. 통계 요약

| 항목 | 개수 | 비고 |
|------|------|------|
| **함수 개수** | 10 | 메인 함수 포함 |
| **IF-THEN 총 개수** | 48 | 기본 32개 + NESTED 16개 |
| **DB조회 총 개수** | 13 | 중복 조회 포함 |
| **IF-THEN + DB조회 Rule** | 13 | 조건부 DB조회 포함 |

---

## 4. 함수별 복잡도 분석

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수* |
|--------|---------|--------|-----------|-------------|
| `요금제_변경체크` | 14 | 5 | 5 | 24 |
| `요금제_가입체크` | 14 | 5 | 5 | 24 |
| `계산위약금` | 7 | 0 | 0 | 7 |
| `요금제_해지체크` | 5 | 3 | 3 | 11 |
| `p100_chk_mndt_item` | 3 | 0 | 0 | 3 |
| `b000_main_proc` | 3 | 0 | 0 | 3 |
| `p200_chk_mtcg_item` | 2 | 0 | 0 | 2 |
| 기타 함수 | 0 | 0 | 0 | 0 |

*복잡도 점수 = IF-THEN + DB조회 + IF+DB Rule

---

## 5. 조회 테이블 통계

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| 고객현재요금제 | 3 | 요금제_변경체크, 요금제_해지체크 (×2) |
| 요금제약정정보 | 2 | 요금제_변경체크, 요금제_해지체크 |
| 요금제마스터 | 2 | 요금제_가입체크, 요금제_변경체크 |
| 요금제판매채널 | 1 | 요금제_가입체크 |
| 단말정보 | 1 | 요금제_가입체크 |
| USIM정보 | 1 | 요금제_가입체크 |
| 요금제판매지역 | 1 | 요금제_가입체크 |
| 요금제변경이력 | 1 | 요금제_변경체크 |
| 요금제변경제한 | 1 | 요금제_변경체크 |
| 고객가입부가서비스 | 1 | 요금제_해지체크 |

**총 테이블 수: 10개**

---

## 6. 버전 정보

- **분석 일자**: 2026-03-31
- **분석자**: Bob (Documentation Writer)
- **문서 버전**: 1.0
- **기반 Pseudocode**: zordms0360120.txt (442 lines, 5,240줄)