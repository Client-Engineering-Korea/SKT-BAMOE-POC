# 모듈 구조 분석: zordms0360050

## 1. 모듈 개요

| 항목 | 내용 |
|------|------|
| **파일명** | zordms0360050.txt |
| **모듈명** | 상품 target_prod_list 정보 set |
| **라인수** | 65줄 |
| **주요 목적** | 상품 정보를 조회하여 출력 구조체(target_prod_list)에 설정 |
| **역할** | 상품 기본 정보 조회, 입력 데이터 정제(trim), target_prod_list 구조체 생성 및 설정, 오류 메시지 처리 |

## 2. 함수 계층 구조

```
zordms0360050 (메인 함수)
│
├── 입력 데이터 trim 처리
│
└── b000_main_proc
    ├── DB조회: pdb_zord_pdr_obj_bas_info_s0005
    │
    ├── 오류 처리
    │   └── m000_msg_append
    │       └── zordms0400340 (메시지 처리)
    │
    └── z900_target_prod_list_set
        └── target_prod_list 배열에 상품 정보 설정
```

## 3. 주요 함수 상세

### 3.1 zordms0360050 (메인 함수)

**설명**: 상품 정보를 조회하여 target_prod_list에 설정하는 진입점

**입력 파라미터**:
- 입력.상품명
- 입력.서비스상품코드
- 입력.상품ID
- 입력.처리일시

**주요 처리 로직**:

1. **입력 데이터 정제** (line 8-9)
   - 입력.상품명 trim 처리
   - 입력.서비스상품코드 trim 처리

2. **메인 처리 호출** (line 11)
   - CALL b000_main_proc(입력)

**반환값**: 성공

---

### 3.2 b000_main_proc (메인 처리)

**설명**: 상품 정보를 DB에서 조회하고 검증

**입력 파라미터**:
- 입력 (전체 입력 객체)

**주요 처리 로직**:

1. **상품 정보 조회** (line 19)
   - 상품정보 ← DB조회("pdb_zord_pdr_obj_bas_info_s0005", 입력.상품ID, 입력.처리일시)

2. **상품 정보 없음 처리** (line 21-25)
   - IF 상품정보 = NOT_FOUND
   - 오류("상품정보 없음")
   - CALL m000_msg_append("ZORDEA099", "E")
   - RETURN 실패

3. **DB 오류 처리** (line 27-31)
   - IF DB오류
   - 오류("DB 조회 오류")
   - CALL m000_msg_append("ZORDEA098", "E")
   - RETURN 실패

4. **target_prod_list 설정** (line 33)
   - CALL z900_target_prod_list_set(입력, 상품정보)

**반환값**: 성공/실패

**에러 처리**:
- ZORDEA099: 상품정보 없음
- ZORDEA098: DB 조회 오류

---

### 3.3 z900_target_prod_list_set (target_prod_list 설정)

**설명**: 조회한 상품 정보를 출력 구조체 배열에 추가

**입력 파라미터**:
- 입력 (입력 객체)
- 상품정보 (DB 조회 결과)

**주요 처리 로직**:

1. **현재 인덱스 획득** (line 41)
   - 인덱스 ← 출력.target_prod_list.카운트

2. **상품 정보 설정** (line 43-49)
   - 출력.target_prod_list[인덱스].상품ID ← 입력.상품ID
   - 출력.target_prod_list[인덱스].상품명 ← 입력.상품명
   - 출력.target_prod_list[인덱스].서비스상품코드 ← 입력.서비스상품코드
   - 출력.target_prod_list[인덱스].가입해지구분코드 ← 입력.가입해지구분코드
   - 출력.target_prod_list[인덱스].처리가능여부 ← 입력.처리가능여부
   - 출력.target_prod_list[인덱스].상품구분코드 ← 상품정보.상품구분코드
   - 출력.target_prod_list[인덱스].상품타입코드 ← 상품정보.상품타입코드

3. **카운트 증가** (line 51)
   - 출력.target_prod_list.카운트 ← 출력.target_prod_list.카운트 + 1

**반환값**: 성공

---

### 3.4 m000_msg_append (메시지 추가)

**설명**: 오류 메시지를 출력 메시지 목록에 추가

**입력 파라미터**:
- 메시지ID (예: "ZORDEA099")
- 메시지타입 (예: "E" - Error)

**주요 처리 로직**:

1. **메시지 처리 모듈 호출** (line 60)
   - CALL zordms0400340(메시지ID, 메시지타입)

**반환값**: 성공

---

## 4. 비즈니스 룰 요약

| Rule ID | Rule 명 | 조건 | 결과 | 비고 |
|---------|---------|------|------|------|
| **Rule 1** | 상품 정보 존재 체크 | 상품정보 = NOT_FOUND | 오류 메시지 "ZORDEA099" | 상품 미등록 |
| **Rule 2** | DB 조회 오류 체크 | DB오류 발생 | 오류 메시지 "ZORDEA098" | 시스템 오류 |
| **Rule 3** | 정상 처리 | 상품정보 존재 AND DB 정상 | target_prod_list에 추가 | 정상 처리 |

**처리 흐름**:
1. 입력 데이터 정제 (trim)
2. 상품 정보 조회
3. 오류 검증
4. target_prod_list 구조체 설정

---

## 5. 데이터베이스 조회

### 5.1 조회 테이블 목록

| 테이블명 | 조회 목적 | 사용 함수 | 조회 키 |
|---------|----------|----------|---------|
| **pdb_zord_pdr_obj_bas_info_s0005** | 상품 기본 정보 조회 | b000_main_proc | 상품ID, 처리일시 |

### 5.2 조회 패턴

**패턴 1: 상품 기본 정보 조회**
- 상품ID + 처리일시로 특정 시점의 상품 정보 조회
- 이력 관리 테이블 (시점별 상품 정보)
- 상품구분코드, 상품타입코드 등 반환

**조회 결과 구조**:
```
상품정보 {
    상품ID: "NA00001234"
    상품구분코드: "G1" | "I2" | "D1"
    상품타입코드: "요금제" | "부가서비스" | "할인"
    ... (기타 상품 속성)
}
```

---

## 6. 처리 흐름도

```
┌─────────────────────────────────────────┐
│      zordms0360050 시작                  │
└────────────────┬────────────────────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ 입력 데이터 trim      │
      │ - 상품명              │
      │ - 서비스상품코드      │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ b000_main_proc       │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ DB: 상품 기본 정보    │
      │ 조회                  │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ 상품정보 존재?        │
      └──────┬───────┬────────┘
             │ NO    │ YES
             │       │
             ▼       ▼
      ┌──────────┐  ┌──────────┐
      │ 오류:    │  │ DB 오류? │
      │ ZORDEA099│  └────┬──┬──┘
      │ 반환실패 │       │  │
      └──────────┘    YES│  │NO
                         │  │
                         ▼  ▼
                  ┌──────────┐  ┌──────────────┐
                  │ 오류:    │  │ z900_target_ │
                  │ ZORDEA098│  │ prod_list_set│
                  │ 반환실패 │  └──────┬───────┘
                  └──────────┘         │
                                       ▼
                            ┌──────────────────┐
                            │ 인덱스 = 카운트  │
                            └────────┬─────────┘
                                     │
                                     ▼
                            ┌──────────────────┐
                            │ target_prod_list │
                            │ [인덱스] 설정:   │
                            │ - 상품ID         │
                            │ - 상품명         │
                            │ - 서비스상품코드 │
                            │ - 가입해지구분   │
                            │ - 처리가능여부   │
                            │ - 상품구분코드   │
                            │ - 상품타입코드   │
                            └────────┬─────────┘
                                     │
                                     ▼
                            ┌──────────────────┐
                            │ 카운트 += 1      │
                            └────────┬─────────┘
                                     │
                                     ▼
                            ┌──────────────────┐
                            │ 성공 반환         │
                            └──────────────────┘
```

---

## 7. 특이사항 및 주의사항

### 7.1 처리 특성

**1. 입력 데이터 정제**
- trim 처리로 공백 제거
- 데이터 품질 향상
- 상품명, 서비스상품코드에 적용

**2. target_prod_list 구조**
- 배열 형태의 출력 구조체
- 여러 상품 정보를 순차적으로 추가
- 카운트로 배열 크기 관리

**3. 오류 메시지 처리**
- 표준화된 메시지 코드 사용
- m000_msg_append를 통한 일관된 처리
- 메시지 타입 구분 (E: Error)

### 7.2 확장성 고려사항

**1. 추가 필드 설정**
```
출력.target_prod_list[인덱스].판매가격 ← 상품정보.판매가격
출력.target_prod_list[인덱스].할인율 ← 상품정보.할인율
```

**2. 배치 처리 지원**
```
FOR EACH 상품 IN 입력.상품목록
    CALL b000_main_proc(상품)
END FOR
```

**3. 검증 로직 추가**
```
IF 입력.상품ID = NULL OR 입력.상품ID = "" THEN
    오류("상품ID 필수")
    RETURN 실패
END IF
```

### 7.3 성능 최적화 포인트

**1. DB 조회 최적화**
- 상품ID + 처리일시 복합 인덱스
- 필요한 컬럼만 SELECT

**2. 배치 조회**
- 여러 상품을 한 번에 조회
- IN 절 사용

**3. 캐싱**
- 상품 기본 정보는 변경 빈도 낮음
- 단기 캐싱 적용 가능

### 7.4 주의사항

**1. 배열 크기 관리**
- target_prod_list 최대 크기 확인
- 배열 오버플로우 방지

**2. 인덱스 동기화**
- 카운트와 실제 배열 크기 일치 확인
- 동시성 처리 시 주의

**3. trim 처리 범위**
- 현재: 상품명, 서비스상품코드
- 필요 시 다른 필드에도 적용

**4. 오류 메시지 코드**
- ZORDEA099: 상품정보 없음
- ZORDEA098: DB 조회 오류
- 메시지 코드 표준 준수

**5. 처리일시 정확성**
- 이력 테이블 조회 시 처리일시 중요
- 시점별 상품 정보 정확히 조회

**6. NULL 처리**
- 상품정보 필드 NULL 체크
- 안전한 데이터 설정

---

## 8. 버전 정보

| 항목 | 내용 |
|------|------|
| **최초 작성일** | 2026-03-31 |
| **작성자** | Bob (Documentation Writer) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | zordms0360050.txt (65 lines) |
| **분석 범위** | 전체 함수 (4개) |