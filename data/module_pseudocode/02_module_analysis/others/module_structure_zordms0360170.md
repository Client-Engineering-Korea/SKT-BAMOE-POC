# 모듈 구조 분석: zordms0360170

## 1. 모듈 개요

| 항목 | 내용 |
|------|------|
| **파일명** | zordms0360170.txt |
| **모듈명** | 상품 할인 계산 |
| **라인수** | 55줄 |
| **주요 목적** | 고객 및 상품에 적용 가능한 모든 할인을 계산하여 최종 금액 산출 |
| **역할** | 할인 정책 조회, 정률/정액 할인 계산, 최대 할인 한도 적용, 중복 할인 처리, 할인 내역 관리 |

## 2. 함수 계층 구조

```
zordms0360170 (메인 함수)
│
├── 기본금액 설정
│
├── DB조회: 할인정책
│
└── FOR EACH 할인 순회
    ├── 적용 가능 여부 체크
    ├── 할인 타입별 계산
    │   ├── IF 할인타입 = "정률" → 기본금액 × 할인율
    │   └── IF 할인타입 = "정액" → 할인금액
    ├── 최대 할인 한도 적용
    ├── 중복 적용 처리
    └── 할인 내역 추가
```

## 3. 주요 함수 상세

### 3.1 zordms0360170 (메인 함수)

**설명**: 적용 가능한 모든 할인을 계산하여 최종 금액 산출

**입력 파라미터**:
- 입력.기본금액
- 입력.고객ID
- 입력.상품ID

**주요 처리 로직**:

1. **초기값 설정** (line 8-9)
   - 기본금액 ← 입력.기본금액
   - 총할인금액 ← 0

2. **할인 정책 조회** (line 11)
   - 할인목록 ← DB조회("할인정책", 입력.고객ID, 입력.상품ID)

3. **할인 목록 순회** (line 13-39)
   - FOR EACH 할인 IN 할인목록

4. **적용 가능 여부 체크** (line 15-17)
   - IF 할인.적용가능 = "N"
   - CONTINUE (다음 할인으로)

5. **할인 타입별 계산** (line 19-23)
   - IF 할인.할인타입 = "정률"
     - 할인금액 ← 기본금액 × (할인.할인율 / 100)
   - ELSE IF 할인.할인타입 = "정액"
     - 할인금액 ← 할인.할인금액

6. **최대 할인 한도 적용** (line 25-27)
   - IF 할인금액 > 할인.최대할인금액
   - 할인금액 ← 할인.최대할인금액

7. **중복 적용 처리** (line 29-35)
   - IF 할인.중복적용 = "N" AND 총할인금액 > 0
     - IF 할인금액 > 총할인금액
       - 총할인금액 ← 할인금액 (더 큰 할인 적용)
   - ELSE
     - 총할인금액 ← 총할인금액 + 할인금액 (누적)

8. **할인 내역 추가** (line 37)
   - 출력.할인내역목록.추가(할인)

9. **최종 금액 계산** (line 41-45)
   - 최종금액 ← 기본금액 - 총할인금액
   - IF 최종금액 < 0 THEN 최종금액 ← 0

10. **결과 설정** (line 47-50)
    - 출력.기본금액 ← 기본금액
    - 출력.총할인금액 ← 총할인금액
    - 출력.최종금액 ← 최종금액
    - 출력.할인건수 ← 출력.할인내역목록.건수

**반환값**: 성공

**특징**:
- 다중 할인 지원
- 중복 적용 제어
- 최대 할인 한도 관리
- 상세한 할인 내역 제공

---

## 4. 비즈니스 룰 요약

| Rule ID | Rule 명 | 조건 | 결과 | 비고 |
|---------|---------|------|------|------|
| **Rule 1** | 정률 할인 | 할인타입 = "정률" | 할인금액 = 기본금액 × 할인율 | 퍼센트 기반 |
| **Rule 2** | 정액 할인 | 할인타입 = "정액" | 할인금액 = 고정 금액 | 금액 기반 |
| **Rule 3** | 최대 할인 한도 | 할인금액 > 최대할인금액 | 할인금액 = 최대할인금액 | 한도 제한 |
| **Rule 4** | 중복 적용 불가 | 중복적용 = "N" AND 기존 할인 존재 | 더 큰 할인만 적용 | 최대값 선택 |
| **Rule 5** | 중복 적용 가능 | 중복적용 = "Y" | 할인 누적 | 합산 |
| **Rule 6** | 최종 금액 보정 | 최종금액 < 0 | 최종금액 = 0 | 음수 방지 |

**할인 적용 순서**:
1. 할인 정책 조회
2. 각 할인별 금액 계산
3. 최대 한도 적용
4. 중복 적용 규칙 적용
5. 최종 금액 = 기본금액 - 총할인금액

---

## 5. 데이터베이스 조회

### 5.1 조회 테이블 목록

| 테이블명 | 조회 목적 | 사용 함수 | 조회 키 |
|---------|----------|----------|---------|
| **할인정책** | 고객 및 상품에 적용 가능한 할인 조회 | zordms0360170 | 고객ID, 상품ID |

### 5.2 조회 패턴

**패턴 1: 할인 정책 조회**
- 고객ID + 상품ID로 적용 가능한 할인 조회
- 1:N 관계 (여러 할인 정책 적용 가능)
- 할인타입, 할인율/할인금액, 중복적용 여부 반환

**할인정책 데이터 구조**:
```
할인정책 {
    할인ID: "DC00001"
    할인명: "VIP 할인"
    적용가능: "Y" | "N"
    할인타입: "정률" | "정액"
    할인율: 10 (정률인 경우)
    할인금액: 5000 (정액인 경우)
    최대할인금액: 10000
    중복적용: "Y" | "N"
}
```

---

## 6. 처리 흐름도

```
┌─────────────────────────────────────────┐
│      zordms0360170 시작                  │
│   (기본금액, 고객ID, 상품ID)             │
└────────────────┬────────────────────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ 초기값 설정           │
      │ 총할인금액 = 0        │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ DB: 할인정책 조회     │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ FOR EACH 할인 순회    │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ 적용가능 = Y?         │
      └──────┬───────┬────────┘
             │ NO    │ YES
             │       │
             ▼       ▼
      ┌──────────┐  ┌──────────────┐
      │ CONTINUE │  │ 할인타입 분기│
      └──────────┘  └──────┬───────┘
                           │
                ┌──────────┴──────────┐
                │                     │
                ▼                     ▼
         ┌─────────────┐      ┌─────────────┐
         │ 정률 할인   │      │ 정액 할인   │
         │ 기본금액×율 │      │ 고정 금액   │
         └──────┬──────┘      └──────┬──────┘
                │                    │
                └────────┬───────────┘
                         │
                         ▼
                ┌────────────────┐
                │ 최대 할인 한도 │
                │ 체크 및 적용   │
                └────────┬───────┘
                         │
                         ▼
                ┌────────────────┐
                │ 중복적용 = N?  │
                └──┬──────────┬──┘
                   │ YES      │ NO
                   │          │
                   ▼          ▼
            ┌──────────┐  ┌──────────┐
            │ 기존할인 │  │ 할인금액 │
            │ 존재?    │  │ 누적     │
            └──┬───┬───┘  └────┬─────┘
               │   │           │
            YES│   │NO         │
               │   │           │
               ▼   ▼           │
         ┌─────────────┐       │
         │ 더 큰 할인  │       │
         │ 선택        │       │
         └──────┬──────┘       │
                │              │
                └──────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ 할인내역 추가   │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ 다음 할인 처리  │
              │ (또는 종료)     │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ 최종금액 계산   │
              │ = 기본금액      │
              │ - 총할인금액    │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ 음수 방지       │
              │ (최소값 = 0)    │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │ 결과 반환       │
              │ (할인 내역)     │
              └────────────────┘
```

---

## 7. 특이사항 및 주의사항

### 7.1 처리 특성

**1. 다중 할인 지원**
- 여러 할인 정책 동시 적용 가능
- 할인 순서대로 처리
- 각 할인 내역 별도 관리

**2. 중복 적용 제어**
- 중복적용 = "Y": 할인 누적
- 중복적용 = "N": 최대 할인만 적용
- 할인 정책별로 설정 가능

**3. 최대 할인 한도**
- 할인 금액 상한선 설정
- 과도한 할인 방지
- 정률 할인에 특히 유용

### 7.2 확장성 고려사항

**1. 할인 우선순위**
```
할인목록 정렬 BY 우선순위 DESC
FOR EACH 할인 IN 할인목록
```

**2. 조건부 할인**
```
IF 할인.최소구매금액 > 0 THEN
    IF 기본금액 < 할인.최소구매금액 THEN
        CONTINUE
    END IF
END IF
```

**3. 할인 조합 제한**
```
IF 할인.배타그룹 EXISTS THEN
    IF 이미적용된할인.배타그룹 = 할인.배타그룹 THEN
        CONTINUE
    END IF
END IF
```

### 7.3 성능 최적화 포인트

**1. 할인 정책 캐싱**
- 고객별 적용 가능 할인 캐싱
- 상품별 할인 정책 캐싱

**2. 조기 종료**
- 최대 할인 도달 시 조기 종료
- 불필요한 할인 계산 생략

**3. 배치 조회**
- 여러 상품의 할인을 한 번에 조회

### 7.4 주의사항

**1. 할인 순서**
- 할인 적용 순서가 결과에 영향
- 중복 불가 할인의 경우 순서 중요
- 우선순위 명확히 정의

**2. 소수점 처리**
- 정률 할인 계산 시 소수점 발생
- 반올림/절사 규칙 명확히
- 최종 금액 정확성 확보

**3. 음수 방지**
- 할인 후 금액이 음수가 되지 않도록
- 최소 금액 0원 보장

**4. 중복 적용 로직**
- 중복 불가 시 최대 할인 선택
- 기존 할인과 비교 필요
- 누적 할인 시 합산

**5. 최대 할인 한도**
- 정률 할인의 경우 필수
- 정액 할인도 한도 설정 가능
- NULL 처리 주의

**6. 할인 내역 관리**
- 모든 적용된 할인 기록
- 감사 및 분석 목적
- 고객 문의 대응

---

## 8. 버전 정보

| 항목 | 내용 |
|------|------|
| **최초 작성일** | 2026-03-31 |
| **작성자** | Bob (Documentation Writer) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | zordms0360170.txt (55 lines) |
| **분석 범위** | 전체 함수 (1개) |