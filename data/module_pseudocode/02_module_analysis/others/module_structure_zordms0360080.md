# 모듈 구조 분석: zordms0360080

## 1. 모듈 개요

| 항목 | 내용 |
|------|------|
| **파일명** | zordms0360080.txt |
| **모듈명** | 상품 가격 정보 조회 및 계산 |
| **라인수** | 48줄 |
| **주요 목적** | 상품의 기본 가격 조회 및 채널별/프로모션 할인 적용 후 최종 가격 계산 |
| **역할** | 상품 가격 조회, 채널별 할인 계산, 프로모션 할인 적용, 부가세 계산, 최종 금액 산출 |

## 2. 함수 계층 구조

```
zordms0360080 (메인 함수)
│
├── DB조회: 상품가격
│
├── 채널별 할인 계산
│   ├── IF 채널 = "온라인" → 5% 할인
│   ├── IF 채널 = "대리점" → 할인 없음
│   └── IF 채널 = "직영점" → 3% 할인
│
├── DB조회: 프로모션
│
├── 프로모션 할인 계산
│   ├── IF 할인타입 = "정률" → 기본가격 × 할인율
│   └── IF 할인타입 = "정액" → 할인금액
│
└── 최종 가격 계산
    ├── 최종가격 = 기본가격 - 채널할인 - 프로모션할인
    ├── 부가세 = 최종가격 × 0.1
    └── 총금액 = 최종가격 + 부가세
```

## 3. 주요 함수 상세

### 3.1 zordms0360080 (메인 함수)

**설명**: 상품 가격을 조회하고 각종 할인을 적용하여 최종 금액을 계산

**입력 파라미터**:
- 입력.상품ID
- 입력.처리일자
- 입력.채널

**주요 처리 로직**:

1. **상품 가격 조회** (line 8)
   - 가격정보 ← DB조회("상품가격", 입력.상품ID, 입력.처리일자)

2. **가격 정보 없음 처리** (line 10-13)
   - IF 가격정보 = NOT_FOUND
   - 오류("가격 정보 없음")
   - RETURN 실패

3. **기본 가격 설정** (line 15)
   - 기본가격 ← 가격정보.기본가격

4. **채널별 할인 계산** (line 17-23)
   - IF 입력.채널 = "온라인"
     - 채널할인 ← 기본가격 × 0.05 (5% 할인)
   - ELSE IF 입력.채널 = "대리점"
     - 채널할인 ← 0 (할인 없음)
   - ELSE IF 입력.채널 = "직영점"
     - 채널할인 ← 기본가격 × 0.03 (3% 할인)

5. **프로모션 조회** (line 25)
   - 프로모션 ← DB조회("프로모션", 입력.상품ID, 현재일자)

6. **프로모션 할인 계산** (line 27-33)
   - IF 프로모션 EXISTS AND 프로모션.활성 = "Y"
     - IF 프로모션.할인타입 = "정률"
       - 프로모션할인 ← 기본가격 × (프로모션.할인율 / 100)
     - ELSE IF 프로모션.할인타입 = "정액"
       - 프로모션할인 ← 프로모션.할인금액

7. **최종 금액 계산** (line 35-43)
   - 최종가격 ← 기본가격 - 채널할인 - 프로모션할인
   - 부가세 ← 최종가격 × 0.1
   - 출력.기본가격 ← 기본가격
   - 출력.채널할인 ← 채널할인
   - 출력.프로모션할인 ← 프로모션할인
   - 출력.최종가격 ← 최종가격
   - 출력.부가세 ← 부가세
   - 출력.총금액 ← 최종가격 + 부가세

**반환값**: 성공/실패

**특징**:
- 다단계 할인 적용 (채널 + 프로모션)
- 부가세 자동 계산
- 상세한 가격 내역 제공

---

## 4. 비즈니스 룰 요약

| Rule ID | Rule 명 | 조건 | 결과 | 비고 |
|---------|---------|------|------|------|
| **Rule 1** | 온라인 채널 할인 | 채널 = "온라인" | 기본가격의 5% 할인 | 온라인 전용 할인 |
| **Rule 2** | 대리점 채널 | 채널 = "대리점" | 할인 없음 | 정가 판매 |
| **Rule 3** | 직영점 채널 할인 | 채널 = "직영점" | 기본가격의 3% 할인 | 직영점 할인 |
| **Rule 4** | 정률 프로모션 | 할인타입 = "정률" AND 활성 = "Y" | 기본가격 × 할인율 | 퍼센트 할인 |
| **Rule 5** | 정액 프로모션 | 할인타입 = "정액" AND 활성 = "Y" | 고정 할인금액 | 금액 할인 |
| **Rule 6** | 부가세 계산 | 항상 | 최종가격의 10% | VAT |

**할인 적용 순서**:
1. 기본가격 조회
2. 채널 할인 적용
3. 프로모션 할인 적용
4. 최종가격 = 기본가격 - 채널할인 - 프로모션할인
5. 부가세 계산
6. 총금액 = 최종가격 + 부가세

---

## 5. 데이터베이스 조회

### 5.1 조회 테이블 목록

| 테이블명 | 조회 목적 | 사용 함수 | 조회 키 |
|---------|----------|----------|---------|
| **상품가격** | 상품 기본 가격 조회 | zordms0360080 | 상품ID, 처리일자 |
| **프로모션** | 활성 프로모션 조회 | zordms0360080 | 상품ID, 현재일자 |

### 5.2 조회 패턴

**패턴 1: 상품 가격 조회**
- 상품ID + 처리일자로 특정 시점의 가격 조회
- 이력 관리 (시점별 가격 변동)
- 기본가격 반환

**패턴 2: 프로모션 조회**
- 상품ID + 현재일자로 활성 프로모션 조회
- 프로모션 기간 내 여부 확인
- 할인타입, 할인율/할인금액 반환

---

## 6. 처리 흐름도

```
┌─────────────────────────────────────────┐
│      zordms0360080 시작                  │
│   (상품ID, 처리일자, 채널)               │
└────────────────┬────────────────────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ DB: 상품가격 조회     │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ 가격정보 존재?        │
      └──────┬───────┬────────┘
             │ NO    │ YES
             │       │
             ▼       ▼
      ┌──────────┐  ┌──────────────┐
      │ 오류반환 │  │ 기본가격 설정│
      └──────────┘  └──────┬───────┘
                           │
                           ▼
                  ┌────────────────┐
                  │ 채널 분기      │
                  └──┬──────┬───┬──┘
                     │      │   │
          ┌──────────┘      │   └──────────┐
          │                 │              │
          ▼                 ▼              ▼
    ┌─────────┐      ┌─────────┐    ┌─────────┐
    │ 온라인  │      │ 대리점  │    │ 직영점  │
    │ 5% 할인 │      │ 0% 할인 │    │ 3% 할인 │
    └────┬────┘      └────┬────┘    └────┬────┘
         │                │              │
         └────────┬───────┴──────────────┘
                  │
                  ▼
         ┌────────────────┐
         │ DB: 프로모션    │
         │ 조회            │
         └────────┬───────┘
                  │
                  ▼
         ┌────────────────┐
         │ 프로모션 존재?  │
         │ AND 활성=Y?     │
         └──┬──────────┬──┘
            │ NO       │ YES
            │          │
            │          ▼
            │   ┌──────────────┐
            │   │ 할인타입 분기│
            │   └──┬────────┬──┘
            │      │        │
            │   ┌──┘        └──┐
            │   │              │
            │   ▼              ▼
            │ ┌─────┐      ┌─────┐
            │ │정률 │      │정액 │
            │ │할인 │      │할인 │
            │ └──┬──┘      └──┬──┘
            │    │            │
            └────┴────────────┘
                  │
                  ▼
         ┌────────────────┐
         │ 최종가격 계산   │
         │ = 기본가격      │
         │ - 채널할인      │
         │ - 프로모션할인  │
         └────────┬───────┘
                  │
                  ▼
         ┌────────────────┐
         │ 부가세 계산     │
         │ = 최종가격×0.1  │
         └────────┬───────┘
                  │
                  ▼
         ┌────────────────┐
         │ 총금액 계산     │
         │ = 최종가격      │
         │ + 부가세        │
         └────────┬───────┘
                  │
                  ▼
         ┌────────────────┐
         │ 결과 반환       │
         │ (가격 내역)     │
         └────────────────┘
```

---

## 7. 특이사항 및 주의사항

### 7.1 처리 특성

**1. 다단계 할인 구조**
- 채널 할인과 프로모션 할인 중복 적용
- 할인 순서: 채널 → 프로모션
- 각 할인 내역 별도 제공

**2. 채널별 차등 할인**
- 온라인: 5% (운영비 절감)
- 직영점: 3% (고객 유치)
- 대리점: 0% (정가 판매)

**3. 프로모션 유연성**
- 정률 할인: 퍼센트 기반
- 정액 할인: 고정 금액
- 활성 여부 체크

### 7.2 확장성 고려사항

**1. 추가 할인 타입**
```
ELSE IF 프로모션.할인타입 = "단계별" THEN
    IF 기본가격 >= 100000 THEN
        프로모션할인 ← 10000
    ELSE IF 기본가격 >= 50000 THEN
        프로모션할인 ← 5000
    END IF
END IF
```

**2. 할인 한도 설정**
```
IF 채널할인 > 최대채널할인 THEN
    채널할인 ← 최대채널할인
END IF
```

**3. 회원 등급별 추가 할인**
```
IF 고객.등급 = "VIP" THEN
    VIP할인 ← 최종가격 × 0.05
    최종가격 ← 최종가격 - VIP할인
END IF
```

### 7.3 성능 최적화 포인트

**1. 가격 정보 캐싱**
- 상품 가격은 자주 변경되지 않음
- 단기 캐싱 적용 (1시간)

**2. 프로모션 캐싱**
- 활성 프로모션 목록 캐싱
- 프로모션 기간 동안 유지

**3. 배치 조회**
- 여러 상품의 가격을 한 번에 조회
- IN 절 사용

### 7.4 주의사항

**1. 가격 정확성**
- 소수점 처리 주의 (반올림/절사)
- 부가세 계산 정확성
- 최종 금액 검증

**2. 할인 중복 적용**
- 채널 할인 + 프로모션 할인 동시 적용
- 할인 순서 명확히 정의
- 최대 할인율 제한 고려

**3. 프로모션 기간**
- 현재일자 기준 활성 프로모션만 적용
- 프로모션 시작일/종료일 체크
- 시간대별 프로모션 고려

**4. 채널 코드 관리**
- 표준화된 채널 코드 사용
- 신규 채널 추가 시 할인율 정의
- 채널 코드 검증

**5. 음수 방지**
- 할인 후 가격이 음수가 되지 않도록
- 최소 가격 설정 고려

**6. 부가세 처리**
- 부가세 포함/별도 옵션
- 면세 상품 처리
- 부가세율 변경 대응

---

## 8. 버전 정보

| 항목 | 내용 |
|------|------|
| **최초 작성일** | 2026-03-31 |
| **작성자** | Bob (Documentation Writer) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | zordms0360080.txt (48 lines) |
| **분석 범위** | 전체 함수 (1개) |