# 모듈 통계 분석: zordms0350280

## 1. 함수 구성

### 함수 개수
- **총 함수 개수**: 1개

### 각 함수별 입력 값

| 함수명 | 입력 파라미터 | 설명 |
|--------|--------------|------|
| [`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) | 입력.기준상품ID | 상품 간 필수/배타/선택 관계 검증 |

---

## 2. Rule 구성

### IF-THEN 개수 (NESTED 포함)

#### 함수별 IF-THEN 개수 테이블

| 함수명 | 기본 IF-THEN | NESTED IF-THEN | 총계 |
|--------|-------------|----------------|------|
| [`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) | 3 | 3 | 6 |
| **전체 합계** | **3** | **3** | **6** |

#### IF-THEN 상세 분석

**[`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) 함수:**

1. **IF-THEN #1** (기본, line 12-17)
   ```
   IF 관계.관계타입 = "필수" THEN
       IF 관계.대상상품 미가입 THEN  [NESTED]
           출력.관계체크결과 ← "N"
           RETURN 성공
   ```

2. **IF-THEN #2** (기본, line 19-24)
   ```
   ELSE IF 관계.관계타입 = "배타" THEN
       IF 관계.대상상품 가입중 THEN  [NESTED]
           출력.관계체크결과 ← "N"
           RETURN 성공
   ```

3. **IF-THEN #3** (기본, line 26-39)
   ```
   ELSE IF 관계.관계타입 = "선택" THEN
       FOR EACH 선택상품 IN 관계.선택상품목록
           IF 선택상품 가입중 THEN  [NESTED]
               선택상품가입수 증가
       IF 선택상품가입수 < 관계.최소선택수 THEN
           출력.관계체크결과 ← "N"
   ```

#### IF-THEN 총계

| 구분 | 개수 |
|------|------|
| 기본 IF-THEN | 3 |
| NESTED IF-THEN | 3 |
| **총 IF-THEN** | **6** |

---

### DB조회 횟수

#### 함수별 DB조회 개수 테이블

| 함수명 | DB조회 횟수 |
|--------|------------|
| [`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) | 1 |
| **전체 합계** | **1** |

#### DB조회 상세 목록

**[`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) 함수:**

1. **DB조회 #1** (line 8)
   - 테이블: `상품관계테이블`
   - 조회 키: `입력.기준상품ID`
   - 목적: 기준 상품의 모든 관계 규칙 조회

---

### IF-THEN + DB조회 Rule

#### 정의 설명
IF-THEN 조건문 내에서 DB조회 결과를 직접 사용하거나, DB조회 직후 그 결과를 조건으로 판단하는 Rule을 의미합니다.

#### 해당 Rule 목록

**[`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) 함수:**

1. **Rule #1** (line 8-17)
   - DB조회: `상품관계테이블` 조회 → FOR EACH 순회
   - IF-THEN: `IF 관계.관계타입 = "필수"` 및 NESTED IF
   - 설명: DB조회 결과를 순회하며 필수 관계 체크

2. **Rule #2** (line 8, 19-24)
   - DB조회: `상품관계테이블` 조회 → FOR EACH 순회
   - IF-THEN: `IF 관계.관계타입 = "배타"` 및 NESTED IF
   - 설명: DB조회 결과를 순회하며 배타 관계 체크

3. **Rule #3** (line 8, 26-39)
   - DB조회: `상품관계테이블` 조회 → FOR EACH 순회
   - IF-THEN: `IF 관계.관계타입 = "선택"` 및 NESTED IF
   - 설명: DB조회 결과를 순회하며 선택 관계 체크

#### IF-THEN + DB조회 Rule 총계

| 함수명 | Rule 개수 |
|--------|----------|
| [`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) | 3 |
| **전체 합계** | **3** |

---

## 3. 통계 요약

| 항목 | 개수 |
|------|------|
| 함수 개수 | 1 |
| IF-THEN 총 개수 | 6 |
| DB조회 총 개수 | 1 |
| IF-THEN + DB조회 Rule 개수 | 3 |

---

## 4. 함수별 복잡도 분석

### 복잡도 점수 계산식
```
복잡도 점수 = IF-THEN 개수 + DB조회 개수 + (IF-THEN + DB조회 Rule 개수)
```

### 함수별 복잡도 점수 테이블

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수 | 복잡도 등급 |
|--------|---------|--------|------------|------------|------------|
| [`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) | 6 | 1 | 3 | 10 | 높음 |

**복잡도 등급 기준:**
- 낮음: 1-3점
- 중간: 4-7점
- 높음: 8-12점
- 매우 높음: 13점 이상

---

## 5. 조회 테이블 통계

### 테이블별 조회 정보

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| `상품관계테이블` | 1 | [`zordms0350280`](../01_module_definition/others/pseudocode_zordms0350280.txt:6) |

### 총 테이블 수
- **조회 대상 테이블 수**: 1개

---

## 6. Rule 패턴 분석

### Rule 유형별 분류

| Rule 유형 | 개수 | 비율 |
|-----------|------|------|
| 관계 타입 분기 (필수/배타/선택) | 3 | 50% |
| NESTED 조건 검증 | 3 | 50% |
| **전체** | **6** | **100%** |

### 복잡도 특성

1. **중첩 구조**
   - 3개의 기본 IF-THEN
   - 3개의 NESTED IF-THEN
   - FOR EACH 루프 내 조건 분기

2. **관계 타입별 처리**
   - 필수: 대상 상품 미가입 체크
   - 배타: 대상 상품 가입 중 체크
   - 선택: 최소 선택 수 체크

3. **Early Return 패턴**
   - 첫 번째 실패 시 즉시 반환
   - 불필요한 검증 생략

### DB 조회 패턴

1. **단일 조회 + 순회 패턴**
   - 1회 DB조회로 모든 관계 획득
   - FOR EACH로 순차 처리
   - 관계 타입별 분기 처리

2. **효율성**
   - 관계 수만큼 반복
   - 조건 불만족 시 즉시 종료
   - 추가 DB조회 없음

---

## 7. 확장성 분석

### 실제 모듈 규모 추정

현재 분석된 모듈은 **상품 관계 검증 모듈**로, 상품 가입/변경 시 필수적으로 호출됩니다.

**예상 호출 빈도:**
- 상품 가입 시: 매번 호출
- 상품 변경 시: 매번 호출
- 예상 일일 호출: 5,000+ 회

### Rule 함수 증가 패턴

**확장 가능 영역:**

1. **추가 관계 타입**
   ```
   ELSE IF 관계.관계타입 = "조건부" THEN
       IF 조건 충족 AND 대상상품 미가입 THEN
           출력.관계체크결과 ← "N"
   ```
   - 예상 증가: +2 IF-THEN (기본 1 + NESTED 1)

2. **관계 우선순위**
   ```
   IF 관계.우선순위 = "높음" THEN
       우선처리()
   ```
   - 예상 증가: +1 IF-THEN

3. **관계 그룹 지원**
   ```
   IF 관계.그룹타입 = "OR" THEN
       FOR EACH 그룹상품
           IF 그룹상품 가입중 THEN
               그룹충족 ← TRUE
   ```
   - 예상 증가: +2 IF-THEN

### 예상 통계 (확장 후)

| 항목 | 현재 | 확장 후 예상 | 증가율 |
|------|------|-------------|--------|
| 함수 개수 | 1 | 2-3 | +100-200% |
| IF-THEN 개수 | 6 | 11-15 | +83-150% |
| DB조회 개수 | 1 | 1-2 | 0-100% |
| 복잡도 | 10 | 15-20 | +50-100% |

---

## 9. 버전 정보

| 항목 | 내용 |
|------|------|
| **분석 일자** | 2026-03-31 |
| **분석자** | Bob (AI Assistant) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | [`zordms0350280.txt`](../01_module_definition/others/pseudocode_zordms0350280.txt) (48 lines) |