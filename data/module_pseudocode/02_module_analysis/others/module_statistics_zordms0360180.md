# 모듈 통계 분석: zordms0360180

## 1. 함수 구성

### 함수 개수
- **총 함수 개수**: 2개

### 각 함수별 입력 값

| 함수명 | 입력 파라미터 | 설명 |
|--------|--------------|------|
| [`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) | 입력.서비스관리번호 | 약정 정보 체크 및 위약금 계산 |
| [`계산위약금`](../01_module_definition/others/pseudocode_zordms0360180.txt:51) | 약정정보, 경과개월수 | 경과 개월수에 따른 위약금 계산 |

---

## 2. Rule 구성

### IF-THEN 개수 (NESTED 포함)

#### 함수별 IF-THEN 개수 테이블

| 함수명 | 기본 IF-THEN | NESTED IF-THEN | 총계 |
|--------|-------------|----------------|------|
| [`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) | 4 | 1 | 5 |
| [`계산위약금`](../01_module_definition/others/pseudocode_zordms0360180.txt:51) | 2 | 0 | 2 |
| **전체 합계** | **6** | **1** | **7** |

#### IF-THEN 상세 분석

**[`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) 함수:**

1. **IF-THEN #1** (기본, line 10-14)
   ```
   IF 약정정보 = NOT_FOUND THEN
       출력.약정존재여부 ← "N"
       출력.약정가능여부 ← "Y"
       RETURN 성공
   ```

2. **IF-THEN #2** (기본, line 18-22)
   ```
   IF 약정정보.약정상태 = "해지" THEN
       출력.약정가능여부 ← "Y"
       출력.메시지 ← "기존 약정 해지됨"
       RETURN 성공
   ```

3. **IF-THEN #3** (기본, line 30-44)
   ```
   IF 남은개월수 > 0 THEN
       위약금 ← 계산위약금(약정정보, 경과개월수)
       IF 위약금 > 0 THEN  [NESTED]
           출력.약정가능여부 ← "N"
       ELSE
           출력.약정가능여부 ← "Y"
   ELSE
       출력.약정가능여부 ← "Y"
       출력.위약금 ← 0
   ```

**[`계산위약금`](../01_module_definition/others/pseudocode_zordms0360180.txt:51) 함수:**

1. **IF-THEN #1** (기본, line 53-59)
   ```
   IF 경과개월수 < 12 THEN
       위약금 ← 약정정보.기본위약금
   ELSE IF 경과개월수 < 24 THEN
       위약금 ← 약정정보.기본위약금 × 0.5
   ELSE
       위약금 ← 0
   ```

#### IF-THEN 총계

| 구분 | 개수 |
|------|------|
| 기본 IF-THEN | 6 |
| NESTED IF-THEN | 1 |
| **총 IF-THEN** | **7** |

---

### DB조회 횟수

#### 함수별 DB조회 개수 테이블

| 함수명 | DB조회 횟수 |
|--------|------------|
| [`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) | 1 |
| [`계산위약금`](../01_module_definition/others/pseudocode_zordms0360180.txt:51) | 0 |
| **전체 합계** | **1** |

#### DB조회 상세 목록

**[`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) 함수:**

1. **DB조회 #1** (line 8)
   - 테이블: `약정정보`
   - 조회 키: `입력.서비스관리번호`
   - 목적: 고객의 약정 상태 및 정보 조회

---

### IF-THEN + DB조회 Rule

#### 정의 설명
IF-THEN 조건문 내에서 DB조회 결과를 직접 사용하거나, DB조회 직후 그 결과를 조건으로 판단하는 Rule을 의미합니다.

#### 해당 Rule 목록

**[`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) 함수:**

1. **Rule #1** (line 8-14)
   - DB조회: `약정정보` 조회
   - IF-THEN: `IF 약정정보 = NOT_FOUND`
   - 설명: DB조회 결과가 없을 경우 약정 미존재 처리

2. **Rule #2** (line 8, 18-22)
   - DB조회: `약정정보` 조회
   - IF-THEN: `IF 약정정보.약정상태 = "해지"`
   - 설명: DB조회 결과의 약정 상태 체크

#### IF-THEN + DB조회 Rule 총계

| 함수명 | Rule 개수 |
|--------|----------|
| [`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) | 2 |
| [`계산위약금`](../01_module_definition/others/pseudocode_zordms0360180.txt:51) | 0 |
| **전체 합계** | **2** |

---

## 3. 통계 요약

| 항목 | 개수 |
|------|------|
| 함수 개수 | 2 |
| IF-THEN 총 개수 | 7 |
| DB조회 총 개수 | 1 |
| IF-THEN + DB조회 Rule 개수 | 2 |

---

## 4. 함수별 복잡도 분석

### 복잡도 점수 계산식
```
복잡도 점수 = IF-THEN 개수 + DB조회 개수 + (IF-THEN + DB조회 Rule 개수)
```

### 함수별 복잡도 점수 테이블

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수 | 복잡도 등급 |
|--------|---------|--------|------------|------------|------------|
| [`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) | 5 | 1 | 2 | 8 | 높음 |
| [`계산위약금`](../01_module_definition/others/pseudocode_zordms0360180.txt:51) | 2 | 0 | 0 | 2 | 낮음 |

**복잡도 등급 기준:**
- 낮음: 1-3점
- 중간: 4-7점
- 높음: 8-12점
- 매우 높음: 13점 이상

---

## 5. 조회 테이블 통계

### 테이블별 조회 정보

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| `약정정보` | 1 | [`zordms0360180`](../01_module_definition/others/pseudocode_zordms0360180.txt:6) |

### 총 테이블 수
- **조회 대상 테이블 수**: 1개

---

## 6. Rule 패턴 분석

### Rule 유형별 분류

| Rule 유형 | 개수 | 비율 |
|-----------|------|------|
| 약정 존재 여부 체크 | 1 | 14.3% |
| 약정 상태 체크 | 1 | 14.3% |
| 남은 개월수 체크 (NESTED) | 2 | 28.6% |
| 경과 개월수별 위약금 계산 | 2 | 28.6% |
| 기타 | 1 | 14.3% |
| **전체** | **7** | **100%** |

### 복잡도 특성

1. **단계별 검증**
   - 약정 존재 → 약정 상태 → 남은 개월수 → 위약금
   - 순차적 체크
   - Early Return 패턴

2. **NESTED 구조**
   - 남은 개월수 체크 내부에 위약금 체크
   - 2단계 조건 검증

3. **위약금 계산 로직**
   - 경과 개월수에 따라 단계별 감소
   - 0~11개월: 100%
   - 12~23개월: 50%
   - 24개월 이상: 0%

### DB 조회 패턴

1. **단일 조회 패턴**
   - 1회 DB조회로 약정 정보 획득
   - 조회 결과 재사용
   - 효율적인 데이터 접근

2. **캐싱 가능성**
   - 약정 정보는 변경 빈도 낮음
   - 메모리 캐싱 적용 가능

---

## 7. 확장성 분석

### 실제 모듈 규모 추정

현재 분석된 모듈은 **약정 정보 체크 모듈**로, 약정 존재 여부 확인 및 위약금을 계산합니다.

**예상 호출 빈도:**
- 약정 변경 시: 매번 호출
- 해지 시: 매번 호출
- 예상 일일 호출: 5,000+ 회

### Rule 함수 증가 패턴

**확장 가능 영역:**

1. **다양한 약정 타입**
   ```
   IF 약정정보.약정타입 = "12개월" THEN
       IF 경과개월수 < 6 THEN
           위약금 ← 약정정보.기본위약금
       ELSE
           위약금 ← 약정정보.기본위약금 × 0.5
   ```
   - 예상 증가: +2 IF-THEN (기본 1 + NESTED 1)

2. **위약금 면제 조건**
   ```
   IF 고객.VIP등급 = "VVIP" OR 특별사유 = "Y" THEN
       위약금 ← 0
   ```
   - 예상 증가: +1 IF-THEN

3. **부분 위약금**
   ```
   위약금 ← 기본위약금 × (남은개월수 / 약정개월수)
   ```
   - 예상 증가: 계산 로직 변경 (IF-THEN 개수 변화 없음)

### 예상 통계 (확장 후)

| 항목 | 현재 | 확장 후 예상 | 증가율 |
|------|------|-------------|--------|
| 함수 개수 | 2 | 3-4 | +50-100% |
| IF-THEN 개수 | 7 | 10-13 | +43-86% |
| DB조회 개수 | 1 | 1-2 | 0-100% |
| 복잡도 (평균) | 5.0 | 7.0 | +40% |

---

## 9. 버전 정보

| 항목 | 내용 |
|------|------|
| **분석 일자** | 2026-03-31 |
| **분석자** | Bob (AI Assistant) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | [`zordms0360180.txt`](../01_module_definition/others/pseudocode_zordms0360180.txt) (65 lines) |