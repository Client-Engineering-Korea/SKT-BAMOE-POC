# 모듈 구조 분석: zordms0360121

## 1. 모듈 개요

| 항목 | 내용 |
|------|------|
| **파일명** | zordms0360121.txt |
| **모듈명** | 무선 기본요금제 기준정보 체크 |
| **라인수** | 90줄 |
| **주요 목적** | 특정 요금제 및 변경 조건에서 요금제 변경 횟수 제한 및 가입해지 제한일 기준을 SKIP 처리 |
| **역할** | 무제한 요금제 변경 조건 체크, 세대간 변경 시 제한 완화, SKIP 플래그 설정 |

## 2. 함수 계층 구조

```
zordms0360121 (메인 함수)
│
├── a000_input_validation (입력 검증)
│   ├── p010_set_initial_val (초기값 설정)
│   ├── p100_chk_mndt_item (필수 항목 체크)
│   └── p200_chk_mtcg_item (선택 항목 체크)
│
├── b000_main_proc (메인 처리)
│   ├── c101_chk1_no_lmt_cnt_prcpln_chg (무제한 요금제 체크1)
│   └── c102_chk2_no_lmt_cnt_prcpln_chg (무제한 요금제 체크2)
│
├── z000_norm_exit_proc (정상 종료)
└── z999_err_exit_proc (오류 종료)
```

## 3. 주요 함수 상세

### 3.1 zordms0360121 (메인 함수)

**설명**: 무선 기본요금제 기준정보 체크 모듈의 진입점

**처리 로직**:
1. 입력 검증 수행 (`a000_input_validation`)
2. 메인 처리 로직 실행 (`b000_main_proc`)
3. 정상 종료 처리 (`z000_norm_exit_proc`)
4. 오류 발생 시 오류 처리 (`z999_err_exit_proc`)

**반환값**: 성공/실패

---

### 3.2 a000_input_validation (입력 검증)

**설명**: 입력 데이터의 유효성을 검증하고 초기값을 설정

**처리 로직**:
1. 초기값 설정 (`p010_set_initial_val`)
2. 필수 항목 체크 (`p100_chk_mndt_item`)
3. 선택 항목 체크 (`p200_chk_mtcg_item`)

**반환값**: 성공

---

### 3.3 b000_main_proc (메인 처리)

**설명**: 요금제 변경 제한 SKIP 조건을 체크

**처리 로직**:
1. 무제한 요금제 체크1 (`c101_chk1_no_lmt_cnt_prcpln_chg`)
2. 무제한 요금제 체크2 (`c102_chk2_no_lmt_cnt_prcpln_chg`)

**반환값**: 성공

---

### 3.4 c101_chk1_no_lmt_cnt_prcpln_chg (무제한 요금제 체크1)

**설명**: 특정 요금제에서 변경 시 제한 SKIP 처리

**주요 처리 로직**:

1. **특정 요금제 체크** (line 41)
   - IF 변경전요금제ID = "NA00006549" OR "NA00006550"

2. **SKIP 플래그 설정** (line 42-43)
   - 출력.요금제변경횟수제한SKIP ← "Y"
   - 출력.가입해지제한일기준SKIP ← "Y"

**반환값**: 성공

**비즈니스 의미**:
- 특정 무제한 요금제(NA00006549, NA00006550)에서 변경 시
- 요금제 변경 횟수 제한 미적용
- 가입해지 제한일 기준 미적용

---

### 3.5 c102_chk2_no_lmt_cnt_prcpln_chg (무제한 요금제 체크2)

**설명**: 세대간 변경 시 제한 SKIP 처리

**주요 처리 로직**:

1. **변경 조건 체크** (line 52)
   - IF (변경코드 = "C1" OR "C2") AND 세대간변경여부 = "Y"

2. **SKIP 플래그 설정** (line 53-54)
   - 출력.요금제변경횟수제한SKIP ← "Y"
   - 출력.가입해지제한일기준SKIP ← "Y"

**반환값**: 성공

**비즈니스 의미**:
- 명의/단말 변경(C1, C2) + 세대간 변경(3G→4G, 4G→5G 등)
- 요금제 변경 횟수 제한 미적용
- 가입해지 제한일 기준 미적용

---

### 3.6 p010_set_initial_val (초기값 설정)

**설명**: 출력 변수 초기화

**주요 처리 로직**:

1. **SKIP 플래그 초기화** (line 63-64)
   - 출력.요금제변경횟수제한SKIP ← "N"
   - 출력.가입해지제한일기준SKIP ← "N"

**반환값**: 성공

---

### 3.7 p100_chk_mndt_item (필수 항목 체크)

**설명**: 필수 입력 항목 검증

**주요 처리 로직**:

1. **서비스관리번호 체크** (line 72-75)
   - IF 서비스관리번호 = NULL
   - 오류("서비스관리번호 필수")
   - RETURN 실패

**반환값**: 성공/실패

---

### 3.8 p200_chk_mtcg_item (선택 항목 체크)

**설명**: 선택 입력 항목 검증

**주요 처리 로직**:

1. **변경코드 유효성 체크** (line 83-86)
   - IF 변경코드 NOT IN ("C1", "C2", "G1", "N1", "M1")
   - 오류("유효하지 않은 변경코드")
   - RETURN 실패

**반환값**: 성공/실패

**변경코드 의미**:
- C1, C2: 명의/단말 변경
- G1: 요금제 변경
- N1: 신규 가입
- M1: 기타 변경

---

## 4. 비즈니스 룰 요약

| Rule ID | Rule 명 | 조건 | 결과 | 비고 |
|---------|---------|------|------|------|
| **Rule 1** | 특정 요금제 제한 완화 | 변경전요금제ID = "NA00006549" OR "NA00006550" | 요금제변경횟수제한SKIP = "Y", 가입해지제한일기준SKIP = "Y" | 무제한 요금제 |
| **Rule 2** | 세대간 변경 제한 완화 | 변경코드 IN ("C1", "C2") AND 세대간변경여부 = "Y" | 요금제변경횟수제한SKIP = "Y", 가입해지제한일기준SKIP = "Y" | 명의/단말 변경 + 세대 변경 |

**SKIP 플래그 의미**:
- **요금제변경횟수제한SKIP = "Y"**: 월 변경 횟수 제한(예: 3회) 미적용
- **가입해지제한일기준SKIP = "Y"**: 최소 사용 기간 제한 미적용

---

## 5. 데이터베이스 조회

### 5.1 조회 테이블 목록

이 모듈은 DB 조회를 수행하지 않습니다. 입력 파라미터만을 사용하여 조건을 체크합니다.

---

## 6. 처리 흐름도

```
┌─────────────────────────────────────────┐
│      zordms0360121 시작                  │
└────────────────┬────────────────────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ a000_input_validation│
      └──────────┬───────────┘
                 │
                 ├─→ p010_set_initial_val
                 │   (SKIP 플래그 = "N")
                 │
                 ├─→ p100_chk_mndt_item
                 │   (서비스관리번호 필수)
                 │
                 └─→ p200_chk_mtcg_item
                     (변경코드 유효성)
                 │
                 ▼
      ┌──────────────────────┐
      │ b000_main_proc       │
      └──────────┬───────────┘
                 │
                 ├─→ c101_chk1_no_lmt_cnt_prcpln_chg
                 │   │
                 │   ▼
                 │   ┌──────────────────┐
                 │   │ 변경전요금제ID?  │
                 │   │ NA00006549 OR    │
                 │   │ NA00006550?      │
                 │   └──┬───────┬───────┘
                 │      │ YES   │ NO
                 │      │       │
                 │      ▼       │
                 │   ┌──────┐  │
                 │   │SKIP  │  │
                 │   │= "Y" │  │
                 │   └──────┘  │
                 │              │
                 └─→ c102_chk2_no_lmt_cnt_prcpln_chg
                     │
                     ▼
                     ┌──────────────────┐
                     │ 변경코드 = C1/C2 │
                     │ AND              │
                     │ 세대간변경 = Y?  │
                     └──┬───────┬───────┘
                        │ YES   │ NO
                        │       │
                        ▼       │
                     ┌──────┐  │
                     │SKIP  │  │
                     │= "Y" │  │
                     └──────┘  │
                                │
                 ┌──────────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │ z000_norm_exit_proc  │
      │ (정상 종료)           │
      └──────────────────────┘
```

---

## 7. 특이사항 및 주의사항

### 7.1 처리 특성

**1. 제한 완화 모듈**
- 일반적인 제한 규칙의 예외 처리
- SKIP 플래그를 통한 제어
- 다른 모듈에서 SKIP 플래그 확인 필요

**2. DB 조회 없음**
- 입력 파라미터만으로 판단
- 빠른 처리 속도
- 외부 의존성 없음

**3. 두 가지 SKIP 조건**
- 특정 요금제 기반
- 변경 유형 + 세대 변경 기반

### 7.2 확장성 고려사항

**1. 추가 요금제 등록**
```
IF 변경전요금제ID IN ("NA00006549", "NA00006550", "NA00006551") THEN
    출력.요금제변경횟수제한SKIP ← "Y"
    출력.가입해지제한일기준SKIP ← "Y"
END IF
```

**2. 추가 SKIP 조건**
```
IF 고객.VIP등급 = "VVIP" THEN
    출력.요금제변경횟수제한SKIP ← "Y"
END IF
```

**3. 부분 SKIP 지원**
```
// 변경 횟수만 SKIP, 제한일은 적용
출력.요금제변경횟수제한SKIP ← "Y"
출력.가입해지제한일기준SKIP ← "N"
```

### 7.3 성능 최적화 포인트

**1. 조건 순서 최적화**
- 빈도가 높은 조건을 먼저 체크
- Early Return 패턴 적용

**2. 요금제 목록 캐싱**
- 특정 요금제 목록을 메모리에 캐싱
- 문자열 비교 대신 Set 조회

### 7.4 주의사항

**1. SKIP 플래그 활용**
- 다른 모듈에서 SKIP 플래그 확인 필수
- SKIP = "Y"일 때 해당 제한 미적용

**2. 요금제 코드 관리**
- NA00006549, NA00006550 코드 변경 시 영향도 분석
- 요금제 폐지 시 코드 제거 필요

**3. 변경코드 표준**
- C1, C2, G1, N1, M1 코드 표준 준수
- 신규 변경코드 추가 시 검증 로직 업데이트

**4. 세대간 변경 판단**
- 세대간변경여부 플래그 정확성 중요
- 3G→4G, 4G→5G 등 세대 변경 정의 명확히

**5. 초기값 설정**
- 항상 "N"으로 초기화
- 조건 충족 시에만 "Y"로 변경

**6. 다른 모듈과의 연계**
- 요금제 변경 모듈에서 SKIP 플래그 확인
- 제한 체크 모듈에서 SKIP 플래그 반영

---

## 8. 버전 정보

| 항목 | 내용 |
|------|------|
| **최초 작성일** | 2026-03-31 |
| **작성자** | Bob (Documentation Writer) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | zordms0360121.txt (90 lines) |
| **분석 범위** | 전체 함수 (8개) |