# 모듈 통계 분석: zordms0360140

## 1. 함수 구성

### 함수 개수
- **총 함수 개수**: 1개

### 각 함수별 입력 값

| 함수명 | 입력 파라미터 | 설명 |
|--------|--------------|------|
| [`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) | 입력.상품ID | 상품 사용 가능 여부 체크 |

---

## 2. Rule 구성

### IF-THEN 개수 (NESTED 포함)

#### 함수별 IF-THEN 개수 테이블

| 함수명 | 기본 IF-THEN | NESTED IF-THEN | 총계 |
|--------|-------------|----------------|------|
| [`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) | 4 | 0 | 4 |
| **전체 합계** | **4** | **0** | **4** |

#### IF-THEN 상세 분석

**[`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) 함수:**

1. **IF-THEN #1** (기본, line 8-11)
   ```
   IF 입력.상품ID = NULL THEN
       오류("상품ID 필수")
       RETURN 실패
   ```

2. **IF-THEN #2** (기본, line 15-19)
   ```
   IF 상품정보.판매가능여부 = "N" THEN
       출력.사용가능여부 ← "N"
       출력.불가사유 ← "판매 중지된 상품"
       RETURN 성공
   ```

3. **IF-THEN #3** (기본, line 21-25)
   ```
   IF 현재일자 NOT BETWEEN 상품정보.판매시작일 AND 판매종료일 THEN
       출력.사용가능여부 ← "N"
       출력.불가사유 ← "판매 기간 아님"
       RETURN 성공
   ```

4. **IF-THEN #4** (기본, line 27-31)
   ```
   IF 상품정보.재고관리 = "Y" AND 상품정보.재고수량 <= 0 THEN
       출력.사용가능여부 ← "N"
       출력.불가사유 ← "재고 부족"
       RETURN 성공
   ```

#### IF-THEN 총계

| 구분 | 개수 |
|------|------|
| 기본 IF-THEN | 4 |
| NESTED IF-THEN | 0 |
| **총 IF-THEN** | **4** |

---

### DB조회 횟수

#### 함수별 DB조회 개수 테이블

| 함수명 | DB조회 횟수 |
|--------|------------|
| [`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) | 1 |
| **전체 합계** | **1** |

#### DB조회 상세 목록

**[`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) 함수:**

1. **DB조회 #1** (line 13)
   - 테이블: `상품마스터`
   - 조회 키: `입력.상품ID`
   - 목적: 상품 기본 정보 및 판매 조건 조회

---

### IF-THEN + DB조회 Rule

#### 정의 설명
IF-THEN 조건문 내에서 DB조회 결과를 직접 사용하거나, DB조회 직후 그 결과를 조건으로 판단하는 Rule을 의미합니다.

#### 해당 Rule 목록

**[`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) 함수:**

1. **Rule #1** (line 13-19)
   - DB조회: `상품마스터` 조회
   - IF-THEN: `IF 상품정보.판매가능여부 = "N"`
   - 설명: DB조회 결과의 판매가능여부 체크

2. **Rule #2** (line 13, 21-25)
   - DB조회: `상품마스터` 조회
   - IF-THEN: `IF 현재일자 NOT BETWEEN 판매시작일 AND 판매종료일`
   - 설명: DB조회 결과의 판매기간 체크

3. **Rule #3** (line 13, 27-31)
   - DB조회: `상품마스터` 조회
   - IF-THEN: `IF 재고관리 = "Y" AND 재고수량 <= 0`
   - 설명: DB조회 결과의 재고 체크

#### IF-THEN + DB조회 Rule 총계

| 함수명 | Rule 개수 |
|--------|----------|
| [`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) | 3 |
| **전체 합계** | **3** |

---

## 3. 통계 요약

| 항목 | 개수 |
|------|------|
| 함수 개수 | 1 |
| IF-THEN 총 개수 | 4 |
| DB조회 총 개수 | 1 |
| IF-THEN + DB조회 Rule 개수 | 3 |

---

## 4. 함수별 복잡도 분석

### 복잡도 점수 계산식
```
복잡도 점수 = IF-THEN 개수 + DB조회 개수 + (IF-THEN + DB조회 Rule 개수)
```

### 함수별 복잡도 점수 테이블

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수 | 복잡도 등급 |
|--------|---------|--------|------------|------------|------------|
| [`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) | 4 | 1 | 3 | 8 | 높음 |

**복잡도 등급 기준:**
- 낮음: 1-3점
- 중간: 4-7점
- 높음: 8-12점
- 매우 높음: 13점 이상

---

## 5. 조회 테이블 통계

### 테이블별 조회 정보

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| `상품마스터` | 1 | [`zordms0360140`](../01_module_definition/others/pseudocode_zordms0360140.txt:6) |

### 총 테이블 수
- **조회 대상 테이블 수**: 1개

---

## 6. Rule 패턴 분석

### Rule 유형별 분류

| Rule 유형 | 개수 | 비율 |
|-----------|------|------|
| 입력 검증 | 1 | 25% |
| 판매가능여부 체크 | 1 | 25% |
| 판매기간 체크 | 1 | 25% |
| 재고 체크 | 1 | 25% |
| **전체** | **4** | **100%** |

### 복잡도 특성

1. **단순 구조**
   - 모든 IF-THEN이 단일 레벨
   - NESTED 구조 없음
   - Early Return 패턴

2. **단계별 검증**
   - 입력 검증 → 판매가능 → 판매기간 → 재고
   - 순차적 체크
   - 첫 실패 시 즉시 반환

3. **명확한 불가 사유**
   - 각 체크마다 구체적인 사유 제공
   - 사용자 친화적

### DB 조회 패턴

1. **단일 조회 패턴**
   - 1회 DB조회로 모든 정보 획득
   - 조회 결과 재사용
   - 효율적인 데이터 접근

2. **캐싱 가능성**
   - 상품마스터 정보는 변경 빈도 낮음
   - 메모리 캐싱 적용 권장

---

## 7. 확장성 분석

### 실제 모듈 규모 추정

현재 분석된 모듈은 **상품 사용 가능 여부 체크 모듈**로, 상품 판매 가능 여부를 종합적으로 검증합니다.

**예상 호출 빈도:**
- 상품 조회 시: 매번 호출
- 장바구니 추가 시: 매번 호출
- 주문 생성 시: 매번 호출
- 예상 일일 호출: 200,000+ 회

### Rule 함수 증가 패턴

**확장 가능 영역:**

1. **추가 체크 항목**
   ```
   IF 상품정보.VIP전용 = "Y" AND 고객.등급 ≠ "VIP" THEN
       출력.사용가능여부 ← "N"
       출력.불가사유 ← "VIP 전용 상품"
       RETURN 성공
   ```
   - 예상 증가: +1 IF-THEN

2. **지역별 판매 제한**
   ```
   IF 상품정보.지역제한 = "Y" THEN
       IF 고객.지역 NOT IN 상품정보.허용지역목록 THEN
           출력.사용가능여부 ← "N"
           출력.불가사유 ← "해당 지역 판매 불가"
           RETURN 성공
   ```
   - 예상 증가: +2 IF-THEN (기본 1 + NESTED 1)

3. **재고 예약 기능**
   ```
   IF 상품정보.재고수량 - 예약수량 <= 0 THEN
       출력.사용가능여부 ← "N"
       출력.불가사유 ← "재고 부족 (예약 포함)"
       RETURN 성공
   ```
   - 예상 증가: +1 IF-THEN, +1 DB조회 (예약 정보)

### 예상 통계 (확장 후)

| 항목 | 현재 | 확장 후 예상 | 증가율 |
|------|------|-------------|--------|
| 함수 개수 | 1 | 2-3 | +100-200% |
| IF-THEN 개수 | 4 | 8-10 | +100-150% |
| DB조회 개수 | 1 | 2-3 | +100-200% |
| 복잡도 | 8 | 13-16 | +63-100% |

---

## 9. 버전 정보

| 항목 | 내용 |
|------|------|
| **분석 일자** | 2026-03-31 |
| **분석자** | Bob (AI Assistant) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | [`zordms0360140.txt`](../01_module_definition/others/pseudocode_zordms0360140.txt) (38 lines) |