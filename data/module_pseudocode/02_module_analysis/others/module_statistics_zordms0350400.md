# 모듈 통계 분석: zordms0350400

## 1. 함수 구성

### 함수 개수
- **총 함수 개수**: 1개

### 각 함수별 입력 값

| 함수명 | 입력 파라미터 | 설명 |
|--------|--------------|------|
| [`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) | 입력.상품ID, 입력.처리구분 | 자동 가입/해지 처리 규칙 실행 |

---

## 2. Rule 구성

### IF-THEN 개수 (NESTED 포함)

#### 함수별 IF-THEN 개수 테이블

| 함수명 | 기본 IF-THEN | NESTED IF-THEN | 총계 |
|--------|-------------|----------------|------|
| [`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) | 3 | 1 | 4 |
| **전체 합계** | **3** | **1** | **4** |

#### IF-THEN 상세 분석

**[`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) 함수:**

1. **IF-THEN #1** (NESTED, line 15-18)
   ```
   FOR EACH 조건 IN 규칙.조건목록
       IF 조건 불만족 THEN  [NESTED in FOR]
           조건충족 ← FALSE
           BREAK
   ```

2. **IF-THEN #2** (기본, line 21-23)
   ```
   IF 조건충족 = FALSE THEN
       CONTINUE
   ```

3. **IF-THEN #3** (기본, line 25-28)
   ```
   IF 규칙.처리타입 = "자동가입" THEN
       FOR EACH 대상상품 IN 규칙.대상상품목록
           출력.자동가입목록.추가(대상상품)
   ```

4. **IF-THEN #4** (기본, line 30-33)
   ```
   ELSE IF 규칙.처리타입 = "자동해지" THEN
       FOR EACH 대상상품 IN 규칙.대상상품목록
           출력.자동해지목록.추가(대상상품)
   ```

#### IF-THEN 총계

| 구분 | 개수 |
|------|------|
| 기본 IF-THEN | 3 |
| NESTED IF-THEN | 1 |
| **총 IF-THEN** | **4** |

---

### DB조회 횟수

#### 함수별 DB조회 개수 테이블

| 함수명 | DB조회 횟수 |
|--------|------------|
| [`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) | 1 |
| **전체 합계** | **1** |

#### DB조회 상세 목록

**[`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) 함수:**

1. **DB조회 #1** (line 8)
   - 테이블: `자동처리규칙`
   - 조회 키: `입력.상품ID, 입력.처리구분`
   - 목적: 상품별 자동처리 규칙 조회

---

### IF-THEN + DB조회 Rule

#### 정의 설명
IF-THEN 조건문 내에서 DB조회 결과를 직접 사용하거나, DB조회 직후 그 결과를 조건으로 판단하는 Rule을 의미합니다.

#### 해당 Rule 목록

**[`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) 함수:**

1. **Rule #1** (line 8-23)
   - DB조회: `자동처리규칙` 조회 → FOR EACH 순회
   - IF-THEN: 조건 검증 및 조건충족 여부 판단
   - 설명: DB조회 결과의 조건 목록을 순회하며 검증

2. **Rule #2** (line 8, 25-28)
   - DB조회: `자동처리규칙` 조회 → FOR EACH 순회
   - IF-THEN: `IF 규칙.처리타입 = "자동가입"`
   - 설명: DB조회 결과를 기반으로 자동가입 처리

3. **Rule #3** (line 8, 30-33)
   - DB조회: `자동처리규칙` 조회 → FOR EACH 순회
   - IF-THEN: `IF 규칙.처리타입 = "자동해지"`
   - 설명: DB조회 결과를 기반으로 자동해지 처리

#### IF-THEN + DB조회 Rule 총계

| 함수명 | Rule 개수 |
|--------|----------|
| [`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) | 3 |
| **전체 합계** | **3** |

---

## 3. 통계 요약

| 항목 | 개수 |
|------|------|
| 함수 개수 | 1 |
| IF-THEN 총 개수 | 4 |
| DB조회 총 개수 | 1 |
| IF-THEN + DB조회 Rule 개수 | 3 |

---

## 4. 함수별 복잡도 분석

### 복잡도 점수 계산식
```
복잡도 점수 = IF-THEN 개수 + DB조회 개수 + (IF-THEN + DB조회 Rule 개수)
```

### 함수별 복잡도 점수 테이블

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수 | 복잡도 등급 |
|--------|---------|--------|------------|------------|------------|
| [`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) | 4 | 1 | 3 | 8 | 높음 |

**복잡도 등급 기준:**
- 낮음: 1-3점
- 중간: 4-7점
- 높음: 8-12점
- 매우 높음: 13점 이상

---

## 5. 조회 테이블 통계

### 테이블별 조회 정보

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| `자동처리규칙` | 1 | [`zordms0350400`](../01_module_definition/others/pseudocode_zordms0350400.txt:6) |

### 총 테이블 수
- **조회 대상 테이블 수**: 1개

---

## 6. Rule 패턴 분석

### Rule 유형별 분류

| Rule 유형 | 개수 | 비율 |
|-----------|------|------|
| 조건 검증 (NESTED) | 1 | 25% |
| 조건충족 여부 판단 | 1 | 25% |
| 처리 타입 분기 | 2 | 50% |
| **전체** | **4** | **100%** |

### 복잡도 특성

1. **다중 루프 구조**
   - 외부 루프: 규칙 목록 순회
   - 내부 루프: 조건 목록 순회
   - 내부 루프: 대상상품 목록 순회

2. **조건 검증 패턴**
   - AND 조건: 모든 조건 충족 필요
   - 하나라도 불만족 시 BREAK
   - 조건충족 플래그 사용

3. **처리 타입별 분기**
   - 자동가입: 대상상품 목록 추가
   - 자동해지: 대상상품 목록 추가
   - 확장 가능한 구조

### DB 조회 패턴

1. **단일 조회 + 다중 순회**
   - 1회 DB조회로 모든 규칙 획득
   - 규칙별 조건 검증
   - 조건 충족 시 처리 실행

2. **효율성**
   - 규칙 수만큼 반복
   - 조건 불만족 시 CONTINUE
   - 추가 DB조회 없음

---

## 7. 확장성 분석

### 실제 모듈 규모 추정

현재 분석된 모듈은 **자동 처리 규칙 실행 모듈**로, 상품 가입/변경/해지 시 자동으로 연관 상품을 처리합니다.

**예상 호출 빈도:**
- 상품 가입 시: 자동처리 규칙 존재 시 호출
- 상품 변경 시: 자동처리 규칙 존재 시 호출
- 상품 해지 시: 자동처리 규칙 존재 시 호출
- 예상 일일 호출: 2,000+ 회

### Rule 함수 증가 패턴

**확장 가능 영역:**

1. **추가 처리 타입**
   ```
   ELSE IF 규칙.처리타입 = "자동변경" THEN
       FOR EACH 대상상품 IN 규칙.대상상품목록
           출력.자동변경목록.추가(대상상품)
   ```
   - 예상 증가: +1 IF-THEN

2. **조건 타입 확장**
   ```
   IF 조건.조건타입 = "복합" THEN
       IF 조건.OR조건 충족 OR 조건.AND조건 충족 THEN
           조건충족 ← TRUE
   ```
   - 예상 증가: +2 IF-THEN (기본 1 + NESTED 1)

3. **우선순위 처리**
   ```
   규칙목록 정렬 BY 우선순위
   IF 규칙.우선순위 = "긴급" THEN
       즉시처리()
   ```
   - 예상 증가: +1 IF-THEN

### 예상 통계 (확장 후)

| 항목 | 현재 | 확장 후 예상 | 증가율 |
|------|------|-------------|--------|
| 함수 개수 | 1 | 2-3 | +100-200% |
| IF-THEN 개수 | 4 | 8-10 | +100-150% |
| DB조회 개수 | 1 | 1-2 | 0-100% |
| 복잡도 | 8 | 12-16 | +50-100% |

---

## 9. 버전 정보

| 항목 | 내용 |
|------|------|
| **분석 일자** | 2026-03-31 |
| **분석자** | Bob (AI Assistant) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | [`zordms0350400.txt`](../01_module_definition/others/pseudocode_zordms0350400.txt) (43 lines) |