# 모듈 통계 분석: zordms0360080

## 1. 함수 구성

### 함수 개수
- **총 함수 개수**: 1개

### 각 함수별 입력 값

| 함수명 | 입력 파라미터 | 설명 |
|--------|--------------|------|
| [`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) | 입력.상품ID, 입력.처리일자, 입력.채널 | 상품 가격 조회 및 할인 계산 |

---

## 2. Rule 구성

### IF-THEN 개수 (NESTED 포함)

#### 함수별 IF-THEN 개수 테이블

| 함수명 | 기본 IF-THEN | NESTED IF-THEN | 총계 |
|--------|-------------|----------------|------|
| [`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) | 4 | 2 | 6 |
| **전체 합계** | **4** | **2** | **6** |

#### IF-THEN 상세 분석

**[`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) 함수:**

1. **IF-THEN #1** (기본, line 10-13)
   ```
   IF 가격정보 = NOT_FOUND THEN
       오류("가격 정보 없음")
       RETURN 실패
   ```

2. **IF-THEN #2** (기본, line 17-23)
   ```
   IF 입력.채널 = "온라인" THEN
       채널할인 ← 기본가격 × 0.05
   ELSE IF 입력.채널 = "대리점" THEN
       채널할인 ← 0
   ELSE IF 입력.채널 = "직영점" THEN
       채널할인 ← 기본가격 × 0.03
   ```

3. **IF-THEN #3** (기본, line 27-33)
   ```
   IF 프로모션 EXISTS AND 프로모션.활성 = "Y" THEN
       IF 프로모션.할인타입 = "정률" THEN  [NESTED]
           프로모션할인 ← 기본가격 × (프로모션.할인율 / 100)
       ELSE IF 프로모션.할인타입 = "정액" THEN  [NESTED]
           프로모션할인 ← 프로모션.할인금액
   ```

#### IF-THEN 총계

| 구분 | 개수 |
|------|------|
| 기본 IF-THEN | 4 |
| NESTED IF-THEN | 2 |
| **총 IF-THEN** | **6** |

---

### DB조회 횟수

#### 함수별 DB조회 개수 테이블

| 함수명 | DB조회 횟수 |
|--------|------------|
| [`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) | 2 |
| **전체 합계** | **2** |

#### DB조회 상세 목록

**[`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) 함수:**

1. **DB조회 #1** (line 8)
   - 테이블: `상품가격`
   - 조회 키: `입력.상품ID, 입력.처리일자`
   - 목적: 상품 기본 가격 조회

2. **DB조회 #2** (line 25)
   - 테이블: `프로모션`
   - 조회 키: `입력.상품ID, 현재일자`
   - 목적: 활성 프로모션 조회

---

### IF-THEN + DB조회 Rule

#### 정의 설명
IF-THEN 조건문 내에서 DB조회 결과를 직접 사용하거나, DB조회 직후 그 결과를 조건으로 판단하는 Rule을 의미합니다.

#### 해당 Rule 목록

**[`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) 함수:**

1. **Rule #1** (line 8-13)
   - DB조회: `상품가격` 조회
   - IF-THEN: `IF 가격정보 = NOT_FOUND`
   - 설명: DB조회 결과가 없을 경우 오류 처리

2. **Rule #2** (line 25-33)
   - DB조회: `프로모션` 조회
   - IF-THEN: `IF 프로모션 EXISTS AND 프로모션.활성 = "Y"`
   - 설명: DB조회 결과 존재 및 활성 여부 확인 후 할인 타입별 처리

#### IF-THEN + DB조회 Rule 총계

| 함수명 | Rule 개수 |
|--------|----------|
| [`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) | 2 |
| **전체 합계** | **2** |

---

## 3. 통계 요약

| 항목 | 개수 |
|------|------|
| 함수 개수 | 1 |
| IF-THEN 총 개수 | 6 |
| DB조회 총 개수 | 2 |
| IF-THEN + DB조회 Rule 개수 | 2 |

---

## 4. 함수별 복잡도 분석

### 복잡도 점수 계산식
```
복잡도 점수 = IF-THEN 개수 + DB조회 개수 + (IF-THEN + DB조회 Rule 개수)
```

### 함수별 복잡도 점수 테이블

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수 | 복잡도 등급 |
|--------|---------|--------|------------|------------|------------|
| [`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) | 6 | 2 | 2 | 10 | 높음 |

**복잡도 등급 기준:**
- 낮음: 1-3점
- 중간: 4-7점
- 높음: 8-12점
- 매우 높음: 13점 이상

---

## 5. 조회 테이블 통계

### 테이블별 조회 정보

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| `상품가격` | 1 | [`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) |
| `프로모션` | 1 | [`zordms0360080`](../01_module_definition/others/pseudocode_zordms0360080.txt:6) |

### 총 테이블 수
- **조회 대상 테이블 수**: 2개

---

## 6. Rule 패턴 분석

### Rule 유형별 분류

| Rule 유형 | 개수 | 비율 |
|-----------|------|------|
| DB조회 결과 검증 | 1 | 16.7% |
| 채널별 할인 분기 | 1 | 16.7% |
| 프로모션 존재 및 활성 체크 | 1 | 16.7% |
| 할인 타입 분기 (NESTED) | 2 | 33.3% |
| 기타 | 1 | 16.7% |
| **전체** | **6** | **100%** |

### 복잡도 특성

1. **다단계 할인 구조**
   - 채널 할인 (3가지 채널)
   - 프로모션 할인 (2가지 타입)
   - 부가세 계산

2. **NESTED 구조**
   - 프로모션 활성 체크 내부에 할인 타입 분기
   - 2단계 조건 검증

3. **다중 DB조회**
   - 상품가격 조회
   - 프로모션 조회
   - 순차적 처리

### DB 조회 패턴

1. **순차 조회 패턴**
   - 기본 가격 조회 → 채널 할인 계산
   - 프로모션 조회 → 프로모션 할인 계산
   - 최종 금액 산출

2. **조건부 조회**
   - 프로모션은 항상 조회하지만
   - 활성 여부에 따라 적용 여부 결정

---

## 7. 확장성 분석

### 실제 모듈 규모 추정

현재 분석된 모듈은 **가격 계산 모듈**로, 상품 가격 조회 및 각종 할인을 적용하여 최종 금액을 계산합니다.

**예상 호출 빈도:**
- 상품 조회 시: 매번 호출
- 장바구니 계산 시: 상품 수만큼 호출
- 주문 생성 시: 매번 호출
- 예상 일일 호출: 100,000+ 회

### Rule 함수 증가 패턴

**확장 가능 영역:**

1. **추가 채널 지원**
   ```
   ELSE IF 입력.채널 = "모바일앱" THEN
       채널할인 ← 기본가격 × 0.07
   ELSE IF 입력.채널 = "제휴사" THEN
       채널할인 ← 기본가격 × 0.04
   ```
   - 예상 증가: +2 IF-THEN

2. **추가 할인 타입**
   ```
   ELSE IF 프로모션.할인타입 = "단계별" THEN
       IF 기본가격 >= 100000 THEN
           프로모션할인 ← 10000
       ELSE IF 기본가격 >= 50000 THEN
           프로모션할인 ← 5000
   ```
   - 예상 증가: +3 IF-THEN (기본 1 + NESTED 2)

3. **회원 등급별 할인**
   ```
   회원등급 ← DB조회("회원정보", 고객ID)
   IF 회원등급.등급 = "VIP" THEN
       VIP할인 ← 최종가격 × 0.05
       최종가격 ← 최종가격 - VIP할인
   ```
   - 예상 증가: +1 IF-THEN, +1 DB조회

### 예상 통계 (확장 후)

| 항목 | 현재 | 확장 후 예상 | 증가율 |
|------|------|-------------|--------|
| 함수 개수 | 1 | 2-3 | +100-200% |
| IF-THEN 개수 | 6 | 12-15 | +100-150% |
| DB조회 개수 | 2 | 3-4 | +50-100% |
| 복잡도 | 10 | 18-23 | +80-130% |

---

## 9. 버전 정보

| 항목 | 내용 |
|------|------|
| **분석 일자** | 2026-03-31 |
| **분석자** | Bob (AI Assistant) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | [`zordms0360080.txt`](../01_module_definition/others/pseudocode_zordms0360080.txt) (49 lines) |