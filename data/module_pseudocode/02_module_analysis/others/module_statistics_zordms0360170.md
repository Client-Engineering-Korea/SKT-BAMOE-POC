# 모듈 통계 분석: zordms0360170

## 1. 함수 구성

### 함수 개수
- **총 함수 개수**: 1개

### 각 함수별 입력 값

| 함수명 | 입력 파라미터 | 설명 |
|--------|--------------|------|
| [`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) | 입력.기본금액, 입력.고객ID, 입력.상품ID | 상품 할인 계산 |

---

## 2. Rule 구성

### IF-THEN 개수 (NESTED 포함)

#### 함수별 IF-THEN 개수 테이블

| 함수명 | 기본 IF-THEN | NESTED IF-THEN | 총계 |
|--------|-------------|----------------|------|
| [`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) | 5 | 2 | 7 |
| **전체 합계** | **5** | **2** | **7** |

#### IF-THEN 상세 분석

**[`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) 함수:**

1. **IF-THEN #1** (기본, line 15-17)
   ```
   IF 할인.적용가능 = "N" THEN
       CONTINUE
   ```

2. **IF-THEN #2** (기본, line 19-23)
   ```
   IF 할인.할인타입 = "정률" THEN
       할인금액 ← 기본금액 × (할인.할인율 / 100)
   ELSE IF 할인.할인타입 = "정액" THEN
       할인금액 ← 할인.할인금액
   ```

3. **IF-THEN #3** (기본, line 25-27)
   ```
   IF 할인금액 > 할인.최대할인금액 THEN
       할인금액 ← 할인.최대할인금액
   ```

4. **IF-THEN #4** (기본, line 29-35)
   ```
   IF 할인.중복적용 = "N" AND 총할인금액 > 0 THEN
       IF 할인금액 > 총할인금액 THEN  [NESTED]
           총할인금액 ← 할인금액
   ELSE
       총할인금액 ← 총할인금액 + 할인금액
   ```

5. **IF-THEN #5** (기본, line 43-45)
   ```
   IF 최종금액 < 0 THEN
       최종금액 ← 0
   ```

#### IF-THEN 총계

| 구분 | 개수 |
|------|------|
| 기본 IF-THEN | 5 |
| NESTED IF-THEN | 2 |
| **총 IF-THEN** | **7** |

---

### DB조회 횟수

#### 함수별 DB조회 개수 테이블

| 함수명 | DB조회 횟수 |
|--------|------------|
| [`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) | 1 |
| **전체 합계** | **1** |

#### DB조회 상세 목록

**[`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) 함수:**

1. **DB조회 #1** (line 11)
   - 테이블: `할인정책`
   - 조회 키: `입력.고객ID, 입력.상품ID`
   - 목적: 고객 및 상품에 적용 가능한 할인 조회

---

### IF-THEN + DB조회 Rule

#### 정의 설명
IF-THEN 조건문 내에서 DB조회 결과를 직접 사용하거나, DB조회 직후 그 결과를 조건으로 판단하는 Rule을 의미합니다.

#### 해당 Rule 목록

**[`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) 함수:**

1. **Rule #1** (line 11-17)
   - DB조회: `할인정책` 조회 → FOR EACH 순회
   - IF-THEN: `IF 할인.적용가능 = "N"`
   - 설명: DB조회 결과의 적용가능 여부 체크

2. **Rule #2** (line 11, 19-23)
   - DB조회: `할인정책` 조회 → FOR EACH 순회
   - IF-THEN: `IF 할인.할인타입 = "정률/정액"`
   - 설명: DB조회 결과의 할인 타입별 계산

3. **Rule #3** (line 11, 25-27)
   - DB조회: `할인정책` 조회 → FOR EACH 순회
   - IF-THEN: `IF 할인금액 > 할인.최대할인금액`
   - 설명: DB조회 결과의 최대할인금액 한도 적용

4. **Rule #4** (line 11, 29-35)
   - DB조회: `할인정책` 조회 → FOR EACH 순회
   - IF-THEN: `IF 할인.중복적용 = "N"` 및 NESTED IF
   - 설명: DB조회 결과의 중복적용 여부에 따른 처리

#### IF-THEN + DB조회 Rule 총계

| 함수명 | Rule 개수 |
|--------|----------|
| [`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) | 4 |
| **전체 합계** | **4** |

---

## 3. 통계 요약

| 항목 | 개수 |
|------|------|
| 함수 개수 | 1 |
| IF-THEN 총 개수 | 7 |
| DB조회 총 개수 | 1 |
| IF-THEN + DB조회 Rule 개수 | 4 |

---

## 4. 함수별 복잡도 분석

### 복잡도 점수 계산식
```
복잡도 점수 = IF-THEN 개수 + DB조회 개수 + (IF-THEN + DB조회 Rule 개수)
```

### 함수별 복잡도 점수 테이블

| 함수명 | IF-THEN | DB조회 | IF+DB Rule | 복잡도 점수 | 복잡도 등급 |
|--------|---------|--------|------------|------------|------------|
| [`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) | 7 | 1 | 4 | 12 | 높음 |

**복잡도 등급 기준:**
- 낮음: 1-3점
- 중간: 4-7점
- 높음: 8-12점
- 매우 높음: 13점 이상

---

## 5. 조회 테이블 통계

### 테이블별 조회 정보

| 테이블명 | 조회 횟수 | 사용 함수 |
|---------|----------|----------|
| `할인정책` | 1 | [`zordms0360170`](../01_module_definition/others/pseudocode_zordms0360170.txt:6) |

### 총 테이블 수
- **조회 대상 테이블 수**: 1개

---

## 6. Rule 패턴 분석

### Rule 유형별 분류

| Rule 유형 | 개수 | 비율 |
|-----------|------|------|
| 적용가능 여부 체크 | 1 | 14.3% |
| 할인 타입 분기 | 1 | 14.3% |
| 최대 할인 한도 | 1 | 14.3% |
| 중복적용 처리 (NESTED) | 2 | 28.6% |
| 음수 방지 | 1 | 14.3% |
| 기타 | 1 | 14.3% |
| **전체** | **7** | **100%** |

### 복잡도 특성

1. **다중 할인 처리**
   - FOR EACH 루프로 여러 할인 순회
   - 할인별 독립적 계산
   - 중복 적용 제어

2. **NESTED 구조**
   - 중복적용 여부 체크 내부에 최대값 선택 로직
   - 2단계 조건 검증

3. **할인 계산 로직**
   - 정률 할인: 퍼센트 기반
   - 정액 할인: 고정 금액
   - 최대 한도 적용
   - 중복 적용 제어

### DB 조회 패턴

1. **단일 조회 + 순회 패턴**
   - 1회 DB조회로 모든 할인 정책 획득
   - FOR EACH로 순차 처리
   - 할인별 조건 검증

2. **효율성**
   - 할인 수만큼 반복
   - 적용 불가 시 CONTINUE
   - 추가 DB조회 없음

---

## 7. 확장성 분석

### 실제 모듈 규모 추정

현재 분석된 모듈은 **할인 계산 모듈**로, 적용 가능한 모든 할인을 계산하여 최종 금액을 산출합니다.

**예상 호출 빈도:**
- 상품 조회 시: 매번 호출
- 장바구니 계산 시: 상품 수만큼 호출
- 주문 생성 시: 매번 호출
- 예상 일일 호출: 150,000+ 회

### Rule 함수 증가 패턴

**확장 가능 영역:**

1. **조건부 할인**
   ```
   IF 할인.최소구매금액 > 0 THEN
       IF 기본금액 < 할인.최소구매금액 THEN
           CONTINUE
   ```
   - 예상 증가: +2 IF-THEN (기본 1 + NESTED 1)

2. **할인 우선순위**
   ```
   할인목록 정렬 BY 우선순위 DESC
   IF 할인.우선순위 = "최우선" THEN
       우선처리()
   ```
   - 예상 증가: +1 IF-THEN

3. **할인 조합 제한**
   ```
   IF 할인.배타그룹 EXISTS THEN
       IF 이미적용된할인.배타그룹 = 할인.배타그룹 THEN
           CONTINUE
   ```
   - 예상 증가: +2 IF-THEN (기본 1 + NESTED 1)

### 예상 통계 (확장 후)

| 항목 | 현재 | 확장 후 예상 | 증가율 |
|------|------|-------------|--------|
| 함수 개수 | 1 | 2-3 | +100-200% |
| IF-THEN 개수 | 7 | 12-15 | +71-114% |
| DB조회 개수 | 1 | 1-2 | 0-100% |
| 복잡도 | 12 | 18-22 | +50-83% |

---

## 9. 버전 정보

| 항목 | 내용 |
|------|------|
| **분석 일자** | 2026-03-31 |
| **분석자** | Bob (AI Assistant) |
| **문서 버전** | 1.0 |
| **기반 Pseudocode** | [`zordms0360170.txt`](../01_module_definition/others/pseudocode_zordms0360170.txt) (56 lines) |